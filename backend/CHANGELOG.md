# Changelog

All notable changes to this project, newest session first. Item numbers refer to the numbered work list agreed with the user (e.g. `[#4a]`).

## 2026-08-12

### Added
- **[#2]** Type-ahead search box in Order Write-Ups' vendor selection (`SelectState` in `src/pages/OrderWriteUps.tsx`), filtering the vendor list by code OR name, case-insensitive. Selection state is keyed by vendor id and untouched by the filter, so narrowing the list never silently deselects an already-picked vendor.
- **[#2]** `code` field added to `VendorListEntry` (`backend/src/api/vendors.ts` and `src/lib/api.ts`) so the frontend can render and search on the raw vendor code separately from its display name. Populated from each `VendorConfig`'s existing `search.vendorCode`.
- **[#1]** New vendor **76863** (Rockwell – Seattle) added to `VENDOR_REGISTRY` (`backend/src/writeUps/shared/vendorRegistry.ts`) — pilot for the new-vendors batch (2N512/Aerotron deferred, too many special rules). REPAIR-default (no BN override), always full flow (`ISSUE_AND_DOCK`), `warrantyEligible: false` — confirmed against the real recording (`discovery-76863-AJS-sn-recording.ts`, found in `C:\Users\717375\Downloads\`, not `backend/` as originally described) that Issue Order + Move to Dock both ran on a non-BN serial, i.e. this vendor simply never uses the warranty path, not a BN-detection edge case. Charge To Account flat `COLLINSDISPATCH100` suffix (Collins-family rule). Purchasing Contact/Conditions/Transportation all inherit the shared family defaults (717375/NET30/FEDEX-2) — the recording's own `232275` was the operator's personal ID, explicitly not a real vendor value, confirmed with the user before use.
- **[#1]** New shared module `backend/src/writeUps/shared/partDetailsReceivingNotes.ts` — the part-level "Details" view (receiving-notes box, `#idContentRow_IdGrpReceivingNotes > td`), distinct from two other same-labeled "Details" views already in this codebase (order-level PO External Reference in `createOrderOnly.ts`; the usage-table "part own details" view in `partOwnDetails.ts`) — never conflated. Gated per-vendor by new `VendorConfig.hasPartDetailsStep` (confirmed present only for 76863 and 1DH10's recordings; confirmed absent from 4X623's, so never assumed universal). Dormant this batch: reads and logs the value but nothing consumes it for a decision yet (that's Aerotron 2N512's rule, deferred).
- **[#1]** RMA framework scaffolding, dormant: `backend/src/writeUps/shared/rmaVendors.ts` (`RMA_VENDOR_IDS` — empty — + `isRmaVendor()`), a new `composeAwaitingRmaNote()` in `createOrderOnly.ts` (reuses the existing `completeCreateOrderOnly`/`verifyExternalReferenceCommitted` order-level External Reference mechanics, just different note text than `composeDoNotShipNote`), and a new `order_created_awaiting_rma` outcome wired through `vendorCodeWriteUp.ts`, `vendorCodeOutcomeLogging.ts`, `api/runLog.ts`, and `db/db.ts`'s outcome union — every exhaustive switch on the outcome type, checked directly rather than assumed. Never fires for any vendor in this batch (empty membership list).

### Fixed (shared engine, not vendor-specific)
- **[#1]** `runVendorCodeWriteUp`'s auth-retry gate (`shared/vendorCodeWriteUp.ts`) was `if (isBnFlow || shipset)` — but `isBnFlow` only reflects whether the BN-prefix *override* fired, and until this batch REPAIR authorization was only ever reached via that override or the shipset case, so the two concepts happened to coincide. 76863 (and the 3 vendors after it) have REPAIR as their *default*, no override involved — `isBnFlow` is false for them even though they need the identical retry-until-APPROVED behavior real BN lines get, not the WARRANTY branch's "REQUESTED is fine" acceptance. Added `isRepairFlow = resolved.authFlow === AUTH_FLOW_REPAIR` (a strict superset of `isBnFlow` — every BN-override line already resolves to REPAIR by construction) and switched the gate to it. Zero behavior change for every existing vendor (0T1Y4 family, Skypaxxx); required for 76863 to reach Issue Order/Move to Dock correctly.

### Changed
- **[#2]** Hardcoded real vendor names as `displayName` for all 10 entries in `backend/src/writeUps/shared/vendorRegistry.ts` (previously placeholder text like `"Vendor 0T1Y4"`), per an explicit code→name mapping supplied by the user. No file path, no env var, no Vendor Assignments sheet parsing, no unmatched-code fallback — the original plan to read this at runtime from the Vendor Assignments spreadsheet was scrapped in favor of a static mapping.
- **[#2]** `OrderWriteUps.tsx`'s vendor rows now render `<code> — <name>` (code in accent color) instead of name alone.

### Fixed
- **[#3]** MXI writer's Playwright browser now launches headless by default (was always `headless: false`, the stray visible-browser icon). `chromium.launch()` in `backend/src/mxiWriter/mxiClient.ts:59` now reads `this.config.headless`. Added `HEADLESS` env var (default `true`; set to the exact literal `"false"` to watch the browser for debugging), documented in `.env.example`. `MxiConfig` gained a `headless` field; both places that construct one (`config.ts`'s `loadMxiConfig()` for the server, and `cliMxiClient.ts` for the `mxi:read-esd`/`mxi:write-esd`/`approve-and-write` CLI tools) now share one `readHeadlessFlag()` helper instead of duplicating the env-var check. Confirmed via grep this is still the only `.launch(` call site in `backend/src`.
- **[#4a]** ESD Finder file ingestion (`backend/src/api/esdFinder/ingestion.ts`) no longer fails real dropped files. Root cause was NOT a title-row-position issue (that theory, from a synthetic test file, didn't hold up against a real one) — it was two separate real bugs, confirmed against a genuine file (`08-12-2026 Brayden Bury OOR.xlsm`, sheet `VendorOrders`, 1933 rows):
  1. **Hardcoded sheet-name lookup.** `readRealHeaders()` called `getWorksheet('Vendor OOR')` / `getWorksheet('CRA OOR')` literally — a real export named its sheet `VendorOrders` instead, so the lookup returned `undefined` and the old code silently handed back an empty header set (every required column then reported "missing," regardless of actual content). Replaced with `resolveAndValidateSheet()`: resolves the real data sheet by CONTENT (whichever worksheet's row-1 headers, after aliasing, satisfy the required column list for the schema being checked), falling back to the first/only worksheet if no sheet matches by content. The silent empty-Set early-return is gone — if a workbook has no worksheets at all, it now throws a clear error naming what it looked for. The previously-planned "scan first 5 rows for a title row" idea was dropped entirely per the user — row 1 is confirmed to always be the true header row.
  2. **Header-name drift.** The real file's CRA-shaped headers (`TAT`, `Order Status`, `Notes`, `RO ESD`) match `CRA_OOR_REQUIRED_HEADERS` exactly except `RO ESD` where the code expects `MXI RO ESD`. Added `CRA_OOR_HEADER_ALIASES` (`backend/src/parsers/craOorParser.ts`) — a small, explicit `{ 'RO ESD': 'MXI RO ESD' }` map — and threaded an optional `headerAliases` parameter through `readSheetRows()` (`sheetUtils.ts`, default `{}`, so every other caller — Vendor OOR, Vendor Assignments, the CLI path — is unaffected) so the alias applies both at validation time AND at actual data-parse time. Verified this matters: without threading it into the parse itself, `parseCraOor`'s `get('MXI RO ESD')` would have returned `null` for every row even after validation passed — the exact "looks complete, one field is silently null" failure mode this project has been burned by before. Confirmed via a synthetic single-row test that the alias genuinely carries a populated value through (`"RO ESD": "15-AUG-2026"` → `mxiRoEsd: "15-AUG-2026"`), not just structurally-present-but-null. The alias is scoped to CRA OOR only — Vendor OOR's own legitimate `RO ESD` column (a different field, the vendor's promised ship date) is never aliased.
  3. **Type detection.** `resolveAndValidateSheet()` also tries the *other* known OOR schema before giving up, so a CRA-shaped file validated under `role="vendor"` now gets a specific "expected VENDOR OOR but this matches CRA OOR instead (sheet X)" error instead of a generic wall of "10 headers missing."
  - Real-file re-test after the fix: `role="cra"` now resolves sheet `VendorOrders` by content, applies the alias, and parses 1933 rows successfully (`role="vendor"` correctly still rejects it, now with the specific cross-schema message). This file's real `RO ESD` column happens to be empty on every row (confirmed independently against the raw sheet, not just the parser output) — so this is a genuine property of the file, not a residual bug.
  - `parseVendorOorFileWithValidation`/`parseCraOorFileWithValidation` also stopped independently re-guessing a hardcoded sheet name for the actual data parse — they now parse the exact sheet validation just resolved.
  - Temp diagnostic logging (gated behind `ESD_FINDER_DEBUG_HEADERS=1`) stays in `ingestion.ts`, moved into `resolveAndValidateSheet()` and fixed to always fire before any resolution decision (previously it sat after the early-return and could be silently skipped in exactly the failure case that mattered).
  - Real vendor data files (`08-12-2026 Brayden Bury OOR.xlsm`, `Week of 08-10-2026 MEASURETECH INC OOR.xlsx`) ended up directly under `backend/` this session (outside the gitignored `data/` dir) — flagged to the user, not staged, `.gitignore` fix or manual move still pending.

### Upcoming
- **[#1]** New vendors + Skypaxxx non-shipset case. Deferred — user will provide a separate, dedicated prompt. Context: the non-shipset case runs all-in-one under a single vendor selection, the same way selecting a vendor code today fires its batch + warranty cases together.
- **[#6]** Security overhaul (login portal, hashed local accounts, encrypted per-user MXI credentials, migration-ready toward Azure). Biggest lift, implemented last. Targets identified: `backend/.env` / `.env.example`, `backend/src/server.ts` (no auth today besides the shared `X-Automation-Key` header), `backend/src/mxiWriter/config.ts` / `cliMxiClient.ts`. Short plan + questions still to be delivered before any code is written, per the user's own instruction on this item.
