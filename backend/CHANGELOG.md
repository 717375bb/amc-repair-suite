# Changelog

All notable changes to this project, newest session first. Item numbers refer to the numbered work list agreed with the user (e.g. `[#4a]`).

## 2026-08-27

### Added — jobs run in parallel across tabs, with live status everywhere
Per explicit user direction: write-ups, the ESD writer and quotes can now run at once, and you can toggle between them while their statuses keep updating.

**The backend already allowed this.** Each of the five job managers has its own module-scoped `activeRunId` and `spawnRunner` has no cap, so one job per tab could already run concurrently. The frontend was the blocker: Order Write-Ups and the ESD Finder had providers mounted in `RequireAuth`, but **Vendor Quotes, Scrapped Parts and the Invoice Price Writer held their `runId` in page-local state** — navigating away unmounted the page, the runId went with it, and polling stopped while the backend job carried on invisibly. The job was fine; the UI forgot.

- **`activeRuns.tsx`** — a registry each tab reports a small summary into and the sidebar renders from. Deliberately a *reporting* registry, not a controller: every tab keeps sole ownership of its polling, phase names and cancel semantics, so a change to one cannot break another.
- **`trackedRun.tsx`** — one generic provider for the three tabs sharing the same lifecycle, including re-attaching to a job already running server-side so a mid-run browser refresh doesn't show an idle tab.
- **The two existing providers were deliberately NOT collapsed into it.** They carry real per-tab complexity — two phases, differing cancel semantics, an incremental log endpoint — that is proven in production; rewriting them would risk a regression for no behavioural gain. They report into the same registry instead, so the sidebar treats all five identically.
- **Sidebar badges** show a spinner plus `N/M`, the backend's own phase word, or a bare "running" — never invented progress. Visible collapsed too, where the spinner alone carries it.
- **Two behaviours were preserved rather than lost in the refactor**, both of which came from real reported bugs:
  - `restart()` — Vendor Quotes and the Invoice Price Writer reuse one `runId` across two phases (ingest→write, and retry). Polling correctly stops at a terminal status, so without an explicit restart the second phase runs blind. That is exactly the "the lines being written still don't appear live" bug; the mechanism is now in the provider rather than duplicated per page.
  - `patchRun()` — the optimistic flip to the write phase, so the progress card appears on the current tick rather than up to 2s later. Only ever anticipates a phase the server has already confirmed by returning 202; never asserts an *outcome*.

### Fixed — SQLite would have failed jobs at random under parallel load
`busy_timeout = 5000` added. WAL already allows readers plus **one** writer, but without a busy timeout a second writer does not wait — it fails immediately with `SQLITE_BUSY`. With five processes now writing audit rows at overlapping moments that would have surfaced as a job dying mid-run for no reason an analyst could act on, and worse, **after** its real MXI write had already landed.

### Added — Aerotron notes carry the part's removal date
Per explicit user direction: the Note To Vendor must read `Removal date: DD-MMM-YYYY` directly above the times and cycles table.

- **The rule that matters**: the event used is *not* the most recent, but **the most recent one whose NAME contains "removal"**. The real captured history for `090520025353A` proves why — it holds an `Installation` on 16-JUN-2026 that is newer than the `Removal` on 04-APR-2026. Sorting by date alone would pick confidently and wrongly.
- **Built against the real DOM, not the recording.** `npm run diag:removal-date` captured the actual page: table `#idTableAdditionalHistory` at `&aTab=Historical.idTabAdditionalHistory`, columns `Event | Recorded By | Reason | Note | Event Date | …`.
- **The Event Date column is located from the HEADER, not a fixed index.** This is not defensive habit — the captured data shows the **Note** column routinely contains dates of its own ("the release date has been modified from 07-NOV-2025 … to 15-APR-2026 …"), so any "first cell holding a date" approach would read the wrong column on those rows.
- **Dates are taken verbatim off the page**, never reformatted through a date library. MXI already renders DD-MMM-YYYY, and the real removal timestamp here is `00:37 EDT` — precisely where a timezone round-trip would render the previous day.
- **`(not found)` means one thing only.** A history that was read and genuinely holds no removal event gets the placeholder, per the user's choice. A history that could **not be read** returns a distinct `removal_date_unreadable` outcome and fails the line — otherwise a broken selector would quietly stamp `(not found)` on every Aerotron note and look exactly like a correct one.
- **Returns the page to the Details tab before finishing.** Load-bearing, not tidying: MXI remembers the active tab per session, so leaving it on Historical would make the *next* part open there too, where `#idTableCurrentUsage` does not exist and the usage read would fail. That exact mechanism caused a real bug in the in-house scrap flow two days earlier.
- Scoped to Aerotron by a per-vendor flag (`needsRemovalDateInNotes`); every other vendor's note is byte-for-byte unchanged, verified by rendering both.

**Two real bugs caught by replaying the reader against the captured page before it ever ran live:**

1. **`__name is not defined`** — const arrow helpers inside `page.evaluate` are wrapped by tsx/esbuild's `keepNames` in a helper that does not exist in the page. The same trap is already documented in `createWorkPackage.ts`, and it was walked into again; the evaluate body is now straight-line code with the constraint restated in place.
2. **A whitespace regex silently degraded to `/s+/g`**, which replaces literal `s` characters — `"Installation of BATTERY"` became `"In tallation of BATTERY"`. It passed the first replay only because `"Removal of BATTERY, APU"` happens to contain no lowercase `s`. Verified fixed both ways.

**Verified** end to end against the real captured production page: 162 event rows parsed, correct answer `27-AUG-2026`, from `Removal of BATTERY, APU (MODEL 40178-24).` — with the note rendering exactly as specified and other vendors' notes unchanged.

### Fixed — the leading-zero merge silently broke two lookups
`ab6658e` renamed two registry ids (`8719` → `08719`, `2750` → `02750`) but left the tables keyed on the old ids.

- **Ducommun stopped being treated as an RMA vendor.** `RMA_VENDOR_IDS` still held `8719`, so `isRmaVendor` returned false for `08719` and those lines would have run the ordinary flow instead of stopping for an RMA.
- **Both vendors lost their CRA purchasing contact**, falling back to the global default `717375` instead of Matthew Fascenda's `238784` — a wrong value written into a real MXI field. Both tables now use the corrected ids.

### Added — vendor 2N512, AEROTRON AIR POWER INC
Same warranty template as the Parker family, with the part-details step. Its CRA row (Matthew Fascenda, `238784`) already existed, so only the registry entry was missing. Now 47 selectable vendors.

### Changed — contract codes apply to every vendor, and carry the CR-prefix
Per explicit user direction: "I don't want this to be done per vendor. I want for each line found within non-aero repair vendors to read the part details notes, and ANY part that sees the whole string FOKKERPBH or PARKERCPH to use that account code."

- **The vendor is no longer consulted at all.** `parkerContractCodes.ts` keyed on a fixed list of Parker codes, which was wrong on the facts — **FOKKERPBH is Aerotron's, not Parker's**. The codes travel with the *contract*, so `contractCodes.ts` replaces it and matches on the notes alone.
- "Non-aero repair vendors" is satisfied **structurally, not by a check**: this module is only ever imported by the vendor-code engine. Aero Repair runs through a separate engine that never touches it.
- **CORRECTED: the account now carries the line's own CR-prefix** — `CR7PARKERCPH`, `CR9FOKKERPBH` — per "a mistake I made yesterday, these accounts DO need the CR7/9 prefix just like normal." It uses the lenient prefix extraction rather than `buildChargeToAccountWithSuffix`, which **throws** on any autofilled value that isn't exactly `<CR-prefix>ROUTINE+NONROUTINE`; a real `CR7HMV` has been seen live, and failing a contract line over the shape of a value being overwritten anyway would be wrong.
- The flow change is unchanged and still applies: contract lines take REPAIR authorization and are issued + moved to dock, rather than stopping at authorization-only.

### Added — approved-base registry, and lines from other bases are skipped
Per explicit user direction: "Sometimes, parts come off at maintenance bases that don't belong to PSA... if a line is from a base other than these, I want that line to be skipped."

- **16 approved bases** in one place (`approvedLocations.ts`): CAK, DCA, PHL, PNS, CVG, DAY, SAV, CLT, DFW, GSP, ORF, TYS route to themselves; **NQA, QRO, CKB, TUS** are handled out of CLT.
- **Skipped at discovery, and reported.** A non-approved base is never offered for write-up and never spends a run slot, but still appears with its reason — so it is visibly different from a line that was simply never found. The location is read off the same grid row the candidate already comes from, so this costs no extra navigation.
- **Re-checked independently at execute time**, before any field is filled, since a line can be selected off a stale snapshot. Reaching that check is itself a signal, and it returns a distinct `base_not_approved` outcome rather than a generic error.
- **An unreadable location is NOT approved.** "We could not tell which base this is" must never become "create the order anyway".
- **Two corrections to yesterday's merged routing**, both confirmed with the user: **TUC → TUS** (TUC is Tucumán, Argentina; TUS is Tucson International), and **PHL now routes to itself** rather than to CLT. The second is a real change to where PHL parts are sent.
- `transformReturnToLocation` now reads its routing from the same registry, so the base a line is *accepted* for can never disagree with the base its order is *created against* — pinned by a test that walks all 16.

**Aero Repair needed no change**: its own 12-station routing table already treats anything outside it as an exception, and all 12 of its stations are on the approved list, so it is already stricter than this rule.

**19 new unit tests, 70 total.**

## 2026-08-26

### Changed — ESD Finder runs from the Vendor OOR file alone
Per explicit user direction: the CRA OOR file and the comparison step are gone. Every field the inference actually reads — `roEsd`, `currentStatus`, `vendorNotes` — comes off the **vendor** row, so the join influenced no decision; it was an extra file to produce and an extra way to fail.

- **`matching/vendorOnlyOrders.ts`** turns vendor rows straight into the shape the inference consumes. `matchOrders(vendorRows, [])` is deliberately **not** reused: it flags every unmatched vendor row `orphaned_vendor_row`, and that match flag becomes the row's ESD flag verbatim in `applyInferenceRules` — so with an empty CRA list every row would have come out non-actionable and the tab would have silently written nothing. Pinned by a regression test that asserts exactly that difference.
- `cra: null` needed no downstream changes — `applyInferenceRules` already read `order.cra?.mxiRoEsd ?? null`. `mxiEsdRaw` and `deltaDaysVsMxi` simply come out null, which is correct when no CRA file stated them.
- **The review table now reads Vendor ESD → Inferred ESD.** The old layout labelled the *inferred* value "Vendor ESD" and sat it next to a Delta column that can no longer have a value; the vendor's own stated ESD (`roEsdRaw`) was not shown at all.
- **`--cra-file` is rejected, not ignored** — a caller still passing it is on an older contract and is told so. The endpoint likewise returns a clear 400 rather than letting multer fail with an opaque "Unexpected field", so a stale browser tab gets an actionable message.
- **The CRA parser and validator are deliberately kept.** The original two-file CLI pipeline (`--file`) still uses them and is untouched by this.

**Verified end to end against the real 495-row OOR file**, with no CRA file: 495 rows ingested, 495 orders built, **all flagged `ok`, zero orphaned**, 495 records out (the index-zip invariant the handoff calls gotcha #3 still holds 1:1), and 12 orders resolved straight off the vendor row's RO ESD with no AI call and no CRA file — which is the point: the vendor file alone carries everything the inference needs. 8 new unit tests, 51 total.

## 2026-08-25

### Added — zero times-and-cycles drafts now land in Outlook Drafts
The draft wording already existed but was delivered by a `mailto:` link, which needs a registered mail handler on the machine and did nothing at all when there wasn't one. It now goes through the same Outlook COM path this project already uses for the quote replies.

- **`POST /api/writeups/maintenance-records-draft`** composes the message and saves it to the analyst's own Drafts. **Recipient and subject are fixed server-side** (`DL_PSA_MaintenanceRecords@psaairlines.com`, `Times and Cycles`) — the client cannot choose who this goes to.
- **One draft per part**, per explicit user choice — the per-line button on each zero-usage exception stays.
- **The part identity moved into the BODY.** It previously lived only in the subject (`Zero Times & Cycles — PN x / SN y`); with the subject now fixed at "Times and Cycles" for every message, that would have left the records team unable to tell which part was meant. A `PN: … SN: …` line now sits above the usage table.
- **Draft only.** `MAINTENANCE_RECORDS_MODE` exists and defaults to `draft`; anything other than the exact string `send` also resolves to `draft`, same strictness and same reasoning as `QUOTE_REPLY_MODE` and `MXI_ENV`. Nothing sets it to `send` today — the path exists so it can be switched on deliberately later, per the user's "draft now, add a send toggle later". An internal DL is still a real email that cannot be unsent.
- **`scripts/create-outlook-mail.ps1` is a separate file from `create-outlook-reply.ps1`**, deliberately. That script replies to an existing thread and can reach external vendors; this one composes a fresh internal message. Keeping them apart leaves the ability to mail an outside party isolated in one small, obvious file.
- **Plain text, not HTML** — the body carries a tab-separated Usage Parm table, and converting to HTML would collapse the tabs and lose the column alignment.
- **The failure is visible now.** A `mailto:` that went nowhere looked identical to one that worked; a drafting failure now reports itself in the row and points at the Copy button as the manual fallback. The body is passed to PowerShell as a UTF-8 temp file rather than a command-line argument, since it contains tabs, newlines and a table.

**Verified end to end against real Outlook**, then cleaned up after itself: the draft landed in `Drafts`, `unsent: true`, subject exactly `Times and Cycles`, recipient resolved to `DL_PSA_MaintenanceRecords`, body carrying both the `PN:` line and the Usage Parm table — and the test draft was deleted, leaving nothing in the mailbox.

**10 more unit tests** (30 total): the exact recipient and subject, the full composed body, a REGRESSION test that the part is named in the body, the tab-separated table shape, non-standard usage parameters (`ADGDeployments`), and a safety test that `Send`, `SEND`, `send `, `true`, `1`, `yes` and `''` all resolve to `draft`. The frontend's "Copy draft" fallback was separately verified to produce **byte-identical** text to the backend, by executing the shipped UI source rather than a copy of it.

### Fixed — the work package rename silently never happened, and was reported as done anyway
User-reported: "It all worked, only thing it didn't do was change the name of the work package from Repair to Scrap." The flow's own audit trail said it had.

- **The cause**: the rename targeted `#idInput10` — a generated, positional id copied from the discovery recording — written as `if (count > 0) { fill }`. When that id did not match, the fill was **skipped silently**, `OK` was clicked anyway, and `renamed work package to "Scrap …"` was pushed into `stepsTaken` **unconditionally**. Everything downstream (schedule, transfer) then succeeded, so the run looked clean while the package was still called `Repair …`.
- **The field is now identified by its CONTENT** — it is the input already holding the package's current name — which survives id changes. `#idInput10` is still tried first (it *is* correct when it matches, and it came from the real recording) but is no longer trusted blindly.
- **Every stage is verified now**: the field must be found, the typed value must read back before `OK` is clicked, and the committed page must actually show the new name. A rename that cannot be confirmed **fails** instead of being reported as done — and if the value does not read back, `OK` is never clicked at all, so the old name is not re-committed under a success message.
- **The name is built by stripping either leading verb**, so a re-run cannot produce "Scrap Scrap …", and MXI's own duplicate marker survives: the real package `Repair (1) LIFEVEST - CREW CRJ (PN: D21344-195, SN: L903140)` becomes `Scrap (1) LIFEVEST - CREW CRJ (PN: D21344-195, SN: L903140)`. Dropping the `(1)` would stop the renamed package matching the one MXI actually created.

**Unit tests extended to 20** (`npm test`), all against the real captured names:

- `toScrapWorkPackageName` — the real captured package renames correctly, the `(1)` marker survives, applying it twice is idempotent, and a name with no leading verb still works.
- `pickWorkPackageNameFieldIndex` — finds the field holding the current name, tolerates whitespace differences between the link text and the field, falls back to shape when the exact name is unknown, refuses to grab an unrelated populated field (`CR7REPAIR`), and **returns -1 rather than guessing** when nothing matches, which is what forces the caller to fail.

**End-to-end behaviour verified in a browser** against a realistic Edit Work Package form, with the name field's id varied to simulate MXI generating a different one:

| | field is `#idInput10` | field id differs |
|---|---|---|
| old code | renamed | **not renamed, reported as renamed** — the bug |
| new code | renamed | renamed |

### Root-caused — MXI remembers the active tab, so the location was read off the wrong one
Second report of `Could not read a base station from this item's current location ("not found")`. This time it is root-caused from real evidence rather than reasoned at, and covered by unit tests.

- **The cause**: MXI remembers the **active tab per session**. This flow itself ends on `aTab=Open.OpenChecks` while reading an item's work packages, so the *next* time an item is opened MXI restores that tab — and the Details content, the only place the item's location is stated, is never rendered at all.
- **Proven from the real capture of the exact serial**: `data/diagnostics/inhouse-scrap-L903140-*.txt`, taken on `aTab=Open.OpenChecks`, contains **no `Location:` label and no station-shaped token anywhere on the page**. The only occurrence of the word is a "Work Location" *column header*. The flow read that page, found nothing, and blamed the part.
- **This is also the real explanation for the earlier "first serial succeeds, every one after it fails" report** — the first serial left the session sitting on the Open tab. The popup leak fixed earlier was real, but it was not that symptom's cause.
- **Fixed** by detecting that the Details tab is not active and selecting it explicitly, then waiting for the `Location:` label itself to appear rather than for a fixed delay. The failure message now includes the URL actually being read.
- **The loose fallback is gone.** The parser no longer falls back to the first bare `XXX/YYY` token anywhere in the body. That fallback is actively dangerous: against a real search-results grid it returns `PNS/STORE` — **another item's location** — which would resolve to a confident set of repair shops for the wrong site and transfer a real part to the wrong place. Failing visibly is correct here.

### Added — unit tests (`npm test`)
The project had no test framework. Now uses Node's built-in runner via tsx, so no new dependency.

- Parsing is split into a pure `inHouseScrapParsing.ts` (`parseCurrentLocation`, `looksLikeDetailsTab`) so it is testable with no browser. This parse has now been the reported bug twice; a regression should surface in milliseconds, not in a live scrap run.
- 10 tests over real production text: the genuine Details-tab rendering of `820CE02Y01/403`, label/value split across lines, mixed-case locations (`PNS/Repair1`), deeper segments (`CLT/USSTG/RACK1`), and end-to-end resolution to `DAY/REPAIR2/SHOP2`.
- Two named **REGRESSION** tests pin the two ways this has actually failed: another item's location off a grid, and a "Work Location" column header being mistaken for the field.
- One test loads the **real L903140 capture from disk** and asserts it does *not* look like the Details tab and yields no location. It throws a pointed error naming the regeneration command if the fixture is ever missing, rather than silently passing.
- **The tests were verified to actually fail on the old behaviour**, not merely to pass on the new: reintroducing the bare-token fallback fails the grid regression test with `actual: 'PNS/STORE'`.

**Flow-level fix verified separately** in a browser against a served page that simulates MXI restoring the previous tab:

| session's restored tab | old behaviour | new behaviour |
|---|---|---|
| Details (fresh session) | `DAY/USSTG` → `DAY/REPAIR2/SHOP2` | same |
| `Open.OpenChecks` (after a prior serial) | **"not found" — the reported bug** | `DAY/USSTG` → `DAY/REPAIR2/SHOP2` |

### Fixed — "no open work package" when the tab click silently never happened
User-reported: the in-house scrap says there's no work package when there plainly is one. Root-caused with `npm run diag:inhouse-scrap`, then reproduced and fixed.

- **The probe passed, and that was the clue.** Run read-only against `L903140` in production it reached `aTab=Open.OpenChecks`, found `input[name="aCheck"]` count `1`, listed the real package (`Repair (1) LIFEVEST - CREW CRJ (PN: D21344-195, SN: L903140)`), and recorded **no** close, crash, popup or dialog. So navigation was sound and the browser never shut down — the difference had to be timing.
- **Root cause**: the two tab clicks were fire-and-forget. `clickIfPresent` waits only **6s** for a link to become *visible*, returns `false` if it doesn't, and **both return values were discarded**. On a slow page the tab was never switched, the flow then read the *Details* tab, found no `aCheck` there, and blamed the part: "No open work package for serial X." This suite has already measured MXI part-detail pages taking **~19s** under load (see the usage-table work in `partOwnDetails.ts`) — which is exactly why a fast read-only probe won the race every time while the real batch lost it.
- **Fixed by confirming the tab actually changed**, with a retry, instead of clicking and hoping. The URL markers are real, captured from production by the probe: `&aTab=Open` after the first click, `&aTab=Open.OpenChecks` after the second. Timeout raised from 6s to 30s, matching the standard used for every other genuine render wait here.
- **"Could not get to the list" is no longer reported as "the list is empty."** Failing to reach the Open Work Packages view now returns its own error naming the URL actually reached, and says plainly that it proves nothing about whether a work package exists.
- **Same class swept through the rest of the flow**: the `Details` tab click gets the same 30s treatment, and the two optional post-transfer `OK` clicks get 15s and now **record which of them fired** in `stepsTaken`. Those are legitimately conditional, but at 6s a slow render reads as "absent" — and a skipped required OK would leave the transfer incomplete while the run reported clean success.
- A closed page is also now its own distinct failure (added alongside the probe), so a browser/session failure can never again be reported as a missing work package.

**Verified** by replaying the real `clickIfPresent` against a served page whose tab links appear after a delay:

| | tabs render fast | tabs render after 12s |
|---|---|---|
| old code | work package found | **"No open work package for serial X"** — the reported bug |
| new code | work package found | work package found |

### Fixed — a work package named anything but "Repair …" got a duplicate created over it
User-reported: the write-up creates a work package even when one already exists, because presence was decided by name format. Per explicit direction — **any value at all in the USSTG line's Work Package column now means a work package exists and the line proceeds**.

- **Presence is read from the Work Package COLUMN, not from the link's name.** It was matched against `/^Repair .*\(PN: …, SN: …\)$/`, so a package named anything else was invisible **twice over**: the no-work-package scanner saw an empty slot and created a duplicate on top of the real one, and the candidate matcher didn't recognise it either, so the line couldn't have been written up regardless. The most obvious case is a package this suite itself renames to `Scrap …` during an in-house scrap, but nothing guarantees `Repair ` is the only prefix MXI or an analyst ever uses.
- **The column is located from the header, not a fixed index**, so adding or removing columns via Options > Display Columns cannot silently shift it. Confirmed against the real captured grid (`data/diagnostics/grid-wait-21844-*.html`): the top header row carries "Work Package" as its own `colspan=1` cell, every cell before it is `colspan=1` too, and the matching data cell is index 6 — empty on that genuinely no-work-package row. If the header cannot be located at all it falls back to the link test rather than risk creating a duplicate off an unreadable row.
- **`REPAIR_LINK_PATTERN` broadened** to require only the `(PN: X, SN|BN: Y)` suffix, so such a line is actually picked up and written up rather than silently skipped. Verified this still matches nothing else on a real row: Part No, Serial No, Owner, Location and the vendor name are all rejected.
- **Aero Repair carries the same change** — its four `^Repair `-anchored patterns (two candidate searches, its own no-work-package scanner, and `navigateToPartGridAndGetCandidates`) no longer require the prefix. The part-number anchor is kept, since that is the safety property that actually matters on a part-filtered grid.
- **`findNoWorkPackageRowForSerial` now delegates to `findNoWorkPackageRowsOnGrid`** instead of keeping a second DOM walk with its own copy of the rule, so the two engines cannot drift on what counts as "has a work package" again.

**A latent runtime bug was caught before shipping.** The first version factored the column lookup into `const` arrow helpers inside `page.evaluate`. tsx/esbuild compiles this file with `keepNames`, which wraps such a function in a `__name(…)` helper that does not exist inside the page — it died with `ReferenceError: __name is not defined`. It would have failed on the first live run. The lookup is now straight-line code with no named helpers, and the constraint is documented in place.

**Verified** by running the real shipped scanner against the real captured production grid, with the Work Package cell patched to hold each name in turn: empty → still correctly reported as no work package (creation still happens where it is genuinely needed); `Repair …`, `Scrap …`, and `WO-12345 whatever the analyst typed` → all three report a work package exists and proceed.

### Fixed — in-house scrap only worked on the first serial in a batch
User-reported: running several serials scraps the first successfully and fails every one after it, always with `Could not read a base station from this item's current location ("not found")`, and **the same serial succeeds when it is first in the list**. Nothing was wrong with the parts — state was carrying between iterations, and the read was landing on the wrong page.

- **Neither location-picker popup was ever closed.** `schedulePopup` and `transferPopup` were each opened via `page.waitForEvent('popup')`, used, and abandoned — so a multi-serial run accumulated **two orphaned MXI pages per part**, each still holding whatever server-side transaction state MXI attaches to a picker. Both are now tracked in outer scope and closed in a `finally`, so they are released on every exit path including the several early returns between them that previously abandoned an open popup.
- **Every serial now starts from a clean browser context.** `page.goto()` resets the main page but does nothing about extra pages left open by the previous serial. The reset loop closes anything that is not the client's own page — directly addressing "there's simply an issue with it moving into the next part."
- **The location read now confirms which page it is on.** The old wait polled the whole body for a bare `XXX/YYY` token and then **swallowed its own timeout**. That failed two ways: it was satisfied by any page carrying a location-shaped token — including the search-results grid — and when it genuinely timed out it fell through silently and reported `"not found"`, which reads like the item has no location rather than "we were never on the item's page". Replaced with the same positive page-identity check proven for the usage-table read: the URL must be the inventory details page, `document.readyState` must be `complete`, and the page must show **this** serial (which also rules out landing on a different item). Failing that now names the URL actually landed on.
- **The location is now anchored on the page's own `Location:` label**, with the bare pattern kept only as a fallback. This is not cosmetic: verification showed that on a search-results grid the old pattern parses `PNS/STORE` — **another item's location** — which would have produced confident candidates for the wrong site. Failing visibly is much better than transferring a real part to the wrong shop.

**Verified** against real page text (taken verbatim from the live `diag:usage-table` probe) served in a browser: a real details page is recognised and parses `DAY/USSTG` → `["DAY/REPAIR2/SHOP2", "DAY/REPAIR1/SHOP1", "DAY/REPAIR"]`; a search-results grid carrying both the serial and other items' locations is correctly **rejected** (the old wait accepted it and would have read `PNS/STORE`); and the stray-page reset takes a context from 3 pages back to 1.

The other scrap paths (`writeVendorScrap`, `writeScrapPriceLines`, `convertToExchange`) were checked for the same popup pattern and have none.

### Root-caused by live probe: the table was there and all-zero; the read was early
`npm run diag:usage-table -- 0t1y4 --env production` settled it. On `820CE02Y01 / 403` — one of the eight failing lines — the probe reported `#idTableCurrentUsage present: true` with real rows, `CYCLES 0 0 0` and `HOURS 0 0 0`, at `DAY/USSTG`. The page was fine and the correct record; the writer was reading it too early. Those values classify as `present_all_zero`, which on a USSTG line routes to `order_created_do_not_ship` — exactly what these same parts did on 2026-08-24.

Three distinct defects, found in order:

1. **A missing table element returned instantly as a confirmed-absent table** (fixed earlier the same day). Genuinely wrong, but not this symptom's cause.
2. **The URL check alone was too weak — a bug introduced by that first fix.** `page.url()` flips the moment a navigation COMMITS, while a server-rendered JSP body arrives much later. The two confirmation polls, 250ms apart, both landed inside that gap, so the writer declared "absent" roughly **500ms after the URL changed** on a page taking seconds to render. Absent now additionally requires `document.readyState === 'complete'` (nothing further is coming for a server-rendered page) **and** the page to actually contain the requested part number and serial — which simultaneously proves real content rendered and rules out having landed on a different inventory record. A genuine BN line satisfies all of this on arrival, so its fast path is preserved.
3. **Probing mid-navigation threw instead of re-polling.** `page.evaluate` against a page that is navigating raises "Execution context was destroyed", and that propagated out as a hard failure. This one was **already in the audit trail** — it quarantined a real Aero Repair line (`5013640`) at 11:56:55 the same morning. A destroyed context is not a fault here; it is the ordinary consequence of the very navigation being waited for, so it is now treated as "not ready yet" and the next poll looks again. Any other error is still re-thrown.

**Verified** by replaying the real shipped `readPartOwnDetails` in a browser against a served page that reproduces the actual race — URL commits immediately, body arrives late — using the live table markup from the probe above:

| case | result |
|---|---|
| body arrives 6s after URL commits | `rows=2 CYCLES=0/0/0 HOURS=0/0/0` (6.6s) |
| body arrives 15s after URL commits | `rows=2 CYCLES=0/0/0 HOURS=0/0/0` (15.5s) |
| body arrives immediately (control) | `rows=2` (425ms) |
| genuine BN line, truly no table (control) | `rows=0, tableFound=false` (622ms — still fast, not stalled) |

Before this change, all three race cases threw or returned a false "absent".

### Still occurring — first diagnosis was wrong; instrumented to settle it
The user re-ran and got the same result on the same 8 lines. **The first fix was not the cause**, and that is now established rather than assumed.

- **The re-run disproves the slow-navigation theory.** Per-line time went from ~14s to ~33s, so the new waiting is definitely running (the fix shipped at 12:16 UTC, the run was 12:31). But **no forced capture was written**, which only happens when `timedOut === false` — meaning the writer *did* reach `InventoryDetails.jsp` and `#idTableCurrentUsage` genuinely was not in the DOM. The element is really missing from the page being read; the open question is now **which page that is**.
- **The previous fix is still correct and stays.** It closed a real hole — a missing table element returning instantly as "confirmed absent" from wherever the browser happened to be — it just was not this symptom's cause. The extra ~19s per line it now spends is real navigation wait that used to be skipped entirely.
- **A second blind spot, and the reason this needed another round trip**: when the table element was missing, the diagnostic capture recorded `(no table element found)` for both its content fields and nothing else. The one case that most needs the surrounding page recorded none of it. `describeMissingTable()` now dumps, on any absent verdict: whether a "Current Usage" heading exists at all, the text and outerHTML around it, **every `<table>` id on the page**, and the first 6000 characters of page text — which is the only way to tell "this part genuinely has no usage" apart from "we opened a different inventory record than we meant to".
- **Captures now fire on every absent verdict**, not just on a timeout. That verdict is rare (44 in the entire history before this) and is exactly the one that was disputed, so it can no longer be reached without the page being recorded.
- **`usage_table_absent_unexpected` now persists its serial number.** It was logged but never written to `filled_fields_json`, so the audit DB could not answer the first question the outcome raises — was this actually a BN line, which legitimately has no usage table?
- **New `npm run diag:usage-table -- <vendorCode> [serialNumber] [--env production]`** reproduces one line's part-details open exactly as the write-up does and dumps the page (text, HTML, screenshot) in about a minute, instead of needing a full batch run to produce evidence. Read-only with respect to MXI data: it ticks the row's inventory checkbox and vendor radio, which is what opening part details requires, and clicks Close afterwards — nothing is filled, submitted, authorized, issued or docked.

**What is known vs. not**: known — the writer reaches the part-details page and the usage table element is absent there; Aero Repair reads the same table fine through the same shared function minutes later; `D98C08-607` had a real table captured on 2026-08-18 and reports absent now. Not yet known — whether the page being read is the intended inventory record. `openPartOwnDetails` clicks the serial-number link **page-wide rather than scoped to the row it just ticked**, which on a vendor-code grid (many parts, many rows) is a materially weaker match than on Aero Repair's single-part grid. That is the leading hypothesis and the probe above is built to confirm or kill it.

### Fixed — "Expected to find times and cycles but no table was there" on every non-BN line
User-reported: the shared vendor-code write-up returned this for all non-BN parts, and "the table is there, definitely." It was. The read was giving a confident wrong answer.

- **Root cause**: `waitForUsageTableResolved`'s exit condition was `if (!last.tableFound || last.rows.length > 0) return`. A **missing** `#idTableCurrentUsage` element returned **immediately** as a *confirmed-absent* table, having waited zero milliseconds. That is only correct for a BN-override line, which genuinely has no Usage Parm table. For every other line it silently converted "the details page hasn't finished loading" into "this part has no times and cycles."
- **The upstream guard couldn't catch it either.** `readPartOwnDetails` waited on `waitForBodyTextIncludes(serialNumber)` — but the **grid** we just clicked from shows the same serial, so that wait passes while the browser is still on the old page. A click that hadn't navigated yet sailed straight through to a probe of the wrong page. This is the same failure the function's own docstring already described for a different symptom in August: a wait satisfied by something that renders earlier than the thing being read.
- **Pinned to that path by timing, not by guesswork**: each of the 8 failing lines took **~14s end to end**, not the 30s+ a real row-parsing timeout costs. The instant-return branch was the only way to be that fast.
- **Corroborating evidence**: `usage_table_absent_unexpected` had **never once occurred** before this run in the whole audit history. In the same 3-minute window the only vendor-code line that succeeded was `903-1341 / BN 397158` — a BN line, which skips the table entirely — and Aero Repair read the table fine through the same shared function at 11:47:56. So this was never a global MXI change.
- **The fix**: "absent" is now only believed once we are positively confirmed to be **on the part-details page**, and the table has been missing on **two consecutive polls**. The URL marker is evidence-backed rather than assumed — all 523 captured usage-table reads in `data/diagnostics` are on `InventoryDetails.jsp`, the 479 successful ones and all 44 genuine BN table-absent ones alike.
- **Read order changed too**: the usage-table wait now runs **first**, because it is the only step that positively establishes which page we are on. `rawText` (which feeds `partDescription`) is read after it, so a slow navigation can no longer have the description parsed off the grid. This also keeps **one** 30s budget for the whole read instead of two stacked ones.
- **The failure is now self-diagnosing.** Two genuinely different causes had been collapsing into one vague outcome; they are now separate errors — `part_details_page_not_reached` (names the URL actually landed on, and states plainly that the table's absence proves nothing) versus `usage_table_rows_empty` (table present, rows didn't parse). A failed read also **always** writes a diagnostic capture now, bypassing the `DIAGNOSTICS_CAPTURE` gate: the eight failures today left no evidence behind at all precisely because that gate was off, which is what made this slow to diagnose. Successful reads stay opt-in.
- **Verified** by replaying the real shipped `readPartOwnDetails` in a real browser against four served pages: details page + real table → 2 rows in 101ms; details page + genuinely no table (BN) → absent in 362ms, unchanged behaviour; **still on the grid (the reported bug)** → now refuses with `part_details_page_not_reached` instead of inventing "absent"; and a details page that arrives 3s late → waits and reads the real rows, where the old code returned "absent" instantly. The forced capture was confirmed written, with the URL and `reachedDetailsPage=false` in its header.

## 2026-08-24

### Added — API reachability diagnostic; connection errors now name their real cause
The same five quote PDFs failed on the corporate network (2026-08-24 14:33 and 15:04) and then succeeded ten minutes later **on a phone hotspot**. An earlier note in this session read that success as evidence *against* a network block; that was wrong, and the reason it was wrong is that it assumed the network was unchanged across the three runs. Same bytes succeeding on a different network is evidence *for* a block on the first one. Corrected here rather than left standing.

- **`describeError()` (`quoteWriter/connectionErrorDetail.ts`)** unwraps the `err.cause` chain the Anthropic SDK hides. Every one of those 10 recorded failures says only `"Connection error."` — the SDK's `APIConnectionError` message — so the audit trail could not tell a DNS failure from a TCP reset from a TLS/certificate rejection, three causes with three completely different fixes. Node puts the real error in `err.cause`, often nested twice (undici wraps its own socket errors); this walks that chain and appends `code`/`syscall`/`hostname`. Now wired into all three AI providers (quote, scrap cert, ESD inference). Verified against synthetic versions of each real failure mode, plus a cyclic cause, a non-Error cause, and no cause at all.
- **`npm run diag:api-reachability`** checks four layers in order — proxy env vars (Node's fetch ignores them, which is a cause in itself), DNS resolution, a direct TLS handshake **including who signed the certificate**, then two API probes shaped like the two real callers: a small Haiku text call (the ESD path) and a real base64 PDF upload to Sonnet (the quote path), using the largest PDF on disk as the worst case.
  - The point is to separate the two hypotheses that the timing evidence alone cannot. **Both probes failing** means a host/domain-level block, which affects ESD inference identically. **Only the PDF probe failing** is the one result that genuinely means "quotes are blocked but ESD is not." The TLS issuer line settles a third: a non-public CA means requests are being decrypted and policy-checked in transit.
  - Read-only, `max_tokens: 16`, no DB/mailbox/MXI access, `maxRetries: 0` so the first real error is reported rather than a retried summary. Safe to run anywhere, repeatedly.
  - **Baseline captured on the hotspot**: DNS resolves, TLS issuer is `Google Trust Services / CN=WE1` (a genuine public CA — no interception), text probe 873ms, PDF probe 2444ms with a 132 KB base64 body. Both shapes work. **The corporate-network run has not been done yet** — that is the comparison that answers the question.
- **Still unresolved either way**: ESD inference has not run since 2026-08-19, so the two paths have never been exercised on the same network at the same time. Quote runs 19 and 20 (08-23, both clean) are of unknown network provenance.

### Fixed — lines with no work package and no assigned task are no longer skipped
User-reported: "The MXI writer is still skipping items with no work package and no assigned tasks. These processes are now included in the script and should not be skipped anymore. This goes for Aero Repair as well." Four separate skips were found; all four are gone.

- **Two one-time proof gates in Aero Repair were stopping every affected line.** `isWorkPackageCreationProven` and `isAdHocContinuationProven` each let the real mutation happen (work package created, Ad-Hoc task created — both independently re-verified) and then **stopped**, waiting on a manual continuation CLI to be run once per environment. The work-package proof file had never been written, so that gate fired on **every** no-work-package line. Both gates, both proof modules, both continuation CLIs, and their npm scripts are removed. Creation and continuation now happen in one pass, and both stay auditable (`workPackageWasCreated` / `unassignedTaskWasAssigned` carry through to every outcome and get their own `write_up_actions` rows).
- **Aero Repair's discovery marked no-assigned-task lines as exceptions**, so they were never even offered as selectable targets — the execute-side fix alone would never have reached them. Now selectable, with the same wording the no-work-package case already used: "No task yet — one will be created automatically."
- **Two vestigial stops in the no-assigned-task branch.** `no_tasks_assigned` (zero non-PC Task-Definition candidates) and `multiple_candidate_tasks` (more than one) were left over from the pre-pivot design, when the task was created *from* the selected candidate. Since the pivot to Ad-Hoc creation, the task's name comes from the line's own Removal Information Task Name/ID read back at the grid — this panel is no longer read at all, so both branches were gating on a list nothing consumes. The vendor-code engine, written after the pivot, never had either check. Aero Repair now matches it. The one real stop that remains is genuinely-missing Removal Information, the only case where a name cannot be composed without fabricating one.
- **The vendor-code engine dropped no-work-package rows silently.** `findCandidateLinesForVendorCodeOnce` only ever collected rows carrying a real `"Repair ..."` link, so a genuine USSTG inventory row with a blank Work Package column vanished — not flagged, not counted, absent from every report. A vendor whose open rows were all in that state resolved to `no_candidate_lines` (**15 real occurrences in the two weeks before this fix**). Aero Repair had solved this months ago; the mechanism was simply never generalized. It is now shared outright (`shared/createWorkPackage.ts`) rather than reimplemented, and the vendor-code path creates the work package, re-reads the grid to confirm the exact composed name, and continues — the same independent re-verification Aero Repair already used.

### Fixed — Create Work Package had never once worked
Found while removing the gates above, **before** any of this ran against MXI.

- `createWorkPackageForLine` located its row with `getByRole('row', { name: rowText, exact: true })`, where `rowText` came from `Element.textContent`. MXI puts inline `<script>`/comment bodies inside its grid rows, so that text reads `"BOOSTER PUMP PRESSURED98C08-607INTERCHG1280PSA (PSA Airlines) DCA/USSTG <!-- function onClick_iButtonSpecialHandling1() ..."`, while an accessible **name** is built from rendered text and excludes all of it. The locator resolved to **0 rows** and the function threw on its very first `.check()`, every time.
- **Independently corroborated**: `write_up_actions` has never recorded a single `work_package_created` row, in any environment, ever. The path had never completed once. It went unnoticed precisely *because* the proof gate stopped every such line immediately afterwards — the gate masked the failure it existed to guard against.
- Fixed by locating the row through its own `input[name="aInventory"]` value — a unique per-inventory `{AES}` token, exact and structural, independent of how MXI renders text.
- **Verified against real captured production DOM** (`data/diagnostics/grid-wait-21844-*.html`, vendor 21844) replayed in a real browser: the row resolves to exactly 1, both the inventory checkbox and the vendor radio check *and read back* checked, `"Create Work Package"` resolves to exactly 1 link (no strict-mode violation), and the composed name comes out `Repair BOOSTER PUMP PRESSURE (PN: D98C08-607, SN: 1280)`. Aero Repair's per-serial variant passes on the same row, and both negative controls (wrong serial, wrong part number) correctly return null rather than falling back to another row.

### Fixed — quote ingest could send an empty message count
`maxMessages` is allowed to sit empty while being typed into, but `handleRun` passed it straight through, so clicking Run before the field blurred sent `""` where a number was required. Pre-existing in uncommitted work and breaking `npm run build`; now falls back to the same default `onBlur` applies.

## 2026-08-23

### Fixed (end of day) — "could not move to dock" was a false negative, not a failed move
- **A test run reported every order as failing the move to dock. The parts had genuinely docked.** All 13 were false negatives from the verification step, not real failures — no rework is needed on those orders.
  - Proven with real evidence rather than by reading the regex: a read-only dump of `P000BFNA`'s outbound shipment `SRRR7001NMKS` shows its Current Location cell reading **`DAY/DOCK`**. `P000BFN9` and `P000BFN8` confirm the same. The move worked; the read of the result did not.
  - Root cause: `readOutboundShipmentDockState()` waited on the text **"Shipment Lines"**, which is the TAB LABEL and renders immediately — well before the grid row underneath it that actually carries the Current Location. The location was read out of a not-yet-populated grid, came back blank, and was honestly reported as `location_unreadable`, which the caller treats as a failed move.
  - Fixed with a bounded content-aware wait for a real location token appearing AFTER the "Shipment Lines" heading, rather than for the heading itself. On timeout it falls through and still reports the blank honestly — a genuinely empty location must not be silently converted into a success.
  - This is the same bug class as the 750ms-wait-before-render fault found in the in-house scrap flow: waiting on a flat delay, or on the first text that appears, instead of on the content actually being read.

### Changed (end of day) — unassigned tasks are now assigned automatically instead of skipped
- **The vendor-code write-up no longer stops on a line whose only problem is an unassigned task.** Per explicit user direction: "It has been explained how to fix that issue, and I don't want those to be skipped anymore." The cost was real — 7 lines skipped for this reason in the last three days alone, across `1DH10`, `0T1Y4`, and `6MXR1`.
  - Mirrors Aero Repair's long-proven path exactly, reusing the same helpers rather than a second implementation: assign the single genuine candidate, then continue into the normal write-up **in the same pass**.
  - **Never trusts the click.** After assigning, it re-opens the view through a fresh navigation and independently re-confirms the task is gone. Anything short of a confirmed-empty state stops the line and says so, rather than proceeding on an unverified assignment.
  - **Still stops when there is more than one candidate.** Ambiguity is not resolved by guessing which task to attach to a real work package — the same line Aero Repair has always drawn.
  - The assignment stays **visible in the run log**: the line's summary is prefixed "Task assigned to work package, then …", matching Aero Repair's wording. An assignment made on the analyst's behalf must never be invisible.

### Added (end of day) — multiple serials per in-house scrap; "soon" badge removed
- **The Scrapped Parts tab is no longer marked "soon"** — the badge and its stale description were still there because an earlier `perl` substitution silently failed to match. Now labelled for what it does.
- **In-house scrap accepts a pasted list of serials**, run one at a time in order. Newline, comma, tab, or semicolon separated, so a column pasted straight out of Excel works.
  - **De-duplicated server-side**, not just in the UI. This is a safety property rather than tidiness: scrapping is irreversible and not idempotent, so the same serial appearing twice in a paste must never become two attempts — the second would try to destroy something already gone. The UI previews the count and says how many duplicates were ignored, but the server's own parse is authoritative.
  - **One serial's failure doesn't stop the rest.** Each is attempted independently, records its own `scrap_outs` row, and reports its own result with its own steps and reason — a batch failure names the part and the cause rather than collapsing into one verdict.
  - `serialAlreadyScrapped()` is re-checked **per serial inside the loop**, not once up front, since an earlier entry in the same batch can change that answer.
  - Cancellation is honoured **between serials, never mid-part** — stopping halfway through one part's sequence would leave it partially processed, which is worse than finishing it.
  - The job model holds a results LIST with a `totalRequested` count, so progress reads "N of M" honestly and a single failure among many can't be hidden behind a later success. The runner still accepts a single `--serial` so the one-off CLI invocation used for the first live test keeps working.

### Added (end of day) — per-action reply wording
- **Four reply templates instead of one**, selected by the MXI action the row ACTUALLY took, so an email can never describe something different from what was written. A repair-worded reply on an order that got converted to an exchange would be worse than no reply at all. Wording supplied verbatim by the user (2026-08-23):
  - `repair` — "...is approved for ."
  - `exchange` — "...is approved as an exchange for ."
  - `scrap_nrep` — "...is approved as a scrap as NREP ."
  - `ber` — "This part is BER, the repair exceeds cost of new. Please send over a quote for the scrap fee."
  - **BER is its own kind, not a scrap variant.** Its wording quotes no price and no order number — it ASKS the vendor for a scrap-fee quote. MXI still gets `BER_DEFAULT_SCRAP_FEE` written provisionally, to be adjusted once the vendor's real figure arrives, which matches the user's "default to 96.00 and I can adjust later".
  - Templates load **lazily per kind and cache separately**, so wording that hasn't been filled in yet blocks only the rows needing it — a batch of ordinary repair quotes no longer goes replyless because the BER wording is still a placeholder. Each file keeps its own not-configured guard.
  - The reply now quotes the amount **actually committed to MXI** rather than the extracted price. They differ on a BER row, where the configured default is written instead of the quote's repair cost.
  - Verified all five routes render cleanly (including exchange-over-NREP), with a check that no placeholder or marker leaks into the text.

### Fixed (end of day) — exchange now overrides NREP
- **A quote that is BOTH vendor-stated NREP and an exchange offer now converts to an exchange, not scrap.** User-reported after testing real exchange quotes: "exchange overrides NREP scraps. When NREP is found but so is exchange, it should only exchange."
  - `resolveWriteAction()` had scrap winning, on my stated reasoning that "a part being scrapped has no unit to exchange". That was backwards. NREP and exchange are both the vendor's own statements in the SAME document, and a vendor saying "your unit is not repairable, here is a replacement" is describing an exchange — the exchange IS how the non-repairable unit gets resolved. Scrapping instead would charge the wrong thing and discard the replacement.
  - Precedence is now explicit, most authoritative first: analyst exclusion, then BER, then exchange, then NREP, then an ordinary repair. **BER deliberately still beats exchange** — it is a human's commercial judgement made after seeing the quote, and outranks anything the model merely read off the page.
  - The frontend's mirrored copy was updated in the same change, and its comment now says explicitly that the two must not drift — otherwise the UI promises one action while the runner performs another.
  - Row badges are now derived from the resolved ACTION rather than the disposition, so an NREP+exchange row reads "Exchange (over NREP)" instead of the flatly wrong "NREP · scrap price". A new banner surfaces the override rather than letting it look like the NREP was missed.
  - Fixed a stale banner while here: it still claimed NREP quotes were "excluded from the MXI write automatically", which stopped being true when NREP was routed to scrap pricing earlier in the day.
  - Verified with an exhaustive 8-case check across every disposition crossed with both exchange states.

### Fixed (end of day) — Move to Dock: the likely root cause, found by accident
- **Uppercase-only location matching almost certainly explains "orders written up and issued but never actually moved to dock."** The morning's verification fix made a false success *visible*; this is the reason it was happening.
  - `readOutboundShipmentDockState()` matched locations with `/\b([A-Z]{3})\/([A-Z0-9]+)\b/` and compared `=== 'USSTG'`. The in-house scrap's first live run then proved, incidentally, that **MXI's own location casing varies by site** — the real picker holds `DFW/REPAIR1/SHOP1` right alongside `PNS/Repair1/Shop1`, `CAK/`, `CLT/`, `GSP/`, `ORF/`, `SAV/`.
  - At any mixed-case site the regex simply did not match. `currentLocationMatch` came back null → `notYetDocked` was **false** → the state read as `already_docked_or_further` → `moveOutboundShipmentToDock()` returned `already_docked_externally` **without ever clicking Move to Dock**. The part never moved, and nothing looked wrong. Verified directly: `PNS/Usstg` and `CAK/Usstg 1 EA` both failed the old test and both pass the new one, while `DFW/USSTG` passes either way — which is exactly why it hit some orders and not others.
  - Also fixes the outbound-shipment identifier: `.endsWith('/DOCK')` was case-sensitive, so a site rendering `/Dock` reported *no outbound shipment* for an order that has one.
  - New `location_unreadable` state. An unreadable page is no longer collapsed into "already docked" — that conflation is what let a real failure read as success. `moveOutboundShipmentToDock()` now attempts the move anyway on that state and lets the post-move verification decide.
  - **Swept the same bug class across the codebase**, since it was never dock-specific: `readCurrentLocationCode()` (threw outright at mixed-case sites), `transformReturnToLocation()`, the `USSTG` line checks in both `aeroRepair/writeUp.ts` and `vendorCodeWriteUp.ts` (which gate the DO-NOT-SHIP diversion), `batchDiscovery.ts`'s station match, and the new in-house scrap's own reads. All location matching is now case-insensitive; a grep confirms none remain.
  - Not yet re-run against a real dock move. `npm run dock:audit` is the way to measure whether the 788 historical `success` rows hold up.

### Fixed (same day) — first live in-house scrap: three real bugs
- **Ran the in-house flow for real against production serial D5300-120 (AED battery pack at PNS/USSTG).** It succeeded on the fourth attempt, having exposed three genuine bugs no amount of code review would have caught. A read-only pre-flight first confirmed the part was a valid candidate.
  - **Ambiguous menu locator.** `/Unserviceable Staging Clerk/i` matched TWO entries — the clerk role and "Unserviceable Staging Clerk Reports" — which Playwright strict mode rejects outright. Caught by the pre-flight, before anything was touched. Now anchored so only the role menu matches.
  - **MXI's own location casing is inconsistent between sites.** The real picker contains both `DFW/REPAIR1/SHOP1` (what the recording used) and `PNS/Repair1/Shop1`, `CAK/Repair1/Shop1`, `CLT/Repair1/Shop1`. An exact case-sensitive match silently failed at every mixed-case site. Matching is now case-insensitive, clicking whatever text MXI actually renders.
  - **The two location popups behave differently.** The SCHEDULE popup lists every repair location directly, and typing "repair" into its find box returns ZERO rows; the TRANSFER popup opens on local store locations (`PNS/STORE/017`...) where the shops only appear after a search — which is why the recording filters there. The picker now tries unfiltered first and only searches if that finds nothing, which is correct for both.
  - **A flat 750ms wait read the inventory page before it rendered**, failing with "could not read a base station" while a 2500ms read of the same page worked. Replaced with a content-aware wait for the location pattern itself.
  - Two further robustness fixes fell out: the work package is now matched on its `(PN: ...)` naming convention rather than a "Repair " prefix (after the rename it starts with "Scrap", and "Scrap Inventory" is an ACTION link that a Scrap-prefix match grabs instead), and the rename strips either prefix so a re-run cannot produce "Scrap Scrap ...".
  - **The location guard proved its worth**: on the first attempt it refused to pick a shop rather than guessing, leaving the part renamed but unscheduled instead of transferred somewhere wrong. All four attempts are in `scrap_outs`, three failed and one successful, with the exact reason each time.
  - End state independently re-verified: no "Repair ..." link remains and the item now reads `PNS/Repair1/Shop1`.

### Added
- **Scrap-out tab (`/scrapped-parts`), replacing the placeholder.** The second scrap step — physically scrapping the part in MXI. Two toggleable modes.
  - **Scrapped at vendor**: drop the vendor's scrap certificate; the order number and serial are read from it, then the full sequence from `discovery-full-scrap-recording.ts` runs — receive shipment, cancel task as SCRAPVEN, complete and sign the work package, inspect as unserviceable, attach the certificate, scrap inventory.
  - **In-house scrap**: no certificate (it's PSA's own decision), just a serial. Follows `discovery-inhouse-scrap-recording.ts`, deriving the repair shop from the item's own current location (`BASE/REPAIR1/SHOP1`, with DAY routing to `DAY/REPAIR2/SHOP2`), and pinning the transfer location to exactly what was scheduled so the two can never disagree.
  - **Both `{AES}` reason tokens resolved from the user's screenshots** to `SCRAP (Scrapped)` and `SCRPV (Vendor Scrapped)`, selected by label. The remaining recorded values (`SCRAPVEN`, `1000600:SCRAP`) were already plain.
  - **Every intermittent step is handled as optional.** The user flagged recording lines 32/38/54 as not always appearing, and the password prompts are equally inconsistent — all go through short bounded waits, so an absent dialog moves the flow on instead of timing out. That exact pattern caused the false failures fixed two days ago.
  - The certificate file is attached by handling BOTH mechanisms — a real `input[type=file]` and an intercepted file chooser — because the recording was made by dragging the file in, which codegen cannot capture.
  - Guards: a document that doesn't read as a genuine scrap certificate scraps nothing; a certificate missing a legible order number or serial refuses rather than guessing; `serialAlreadyScrapped()` blocks a second scrap of the same serial, since scrapping is irreversible and not idempotent. The part-specific `"Repair ..."` link is matched on its prefix rather than the recording's literal description.
  - Every run records which steps actually fired, so an intermittent branch is auditable rather than invisible. Endpoints verified live returning 401 unauthenticated.
  - **Not yet run against a real part.**

- **Scrap pricing (`npm run scrap:price`).** The first of the two scrap steps — what happens when a vendor quotes a scrap fee rather than a repair. Adds a miscellaneous SCRAP line at the quoted fee, zeroes the original line (per user direction, "the scrap gets all the cost"), sets BOTH lines to type SCRAP and Promised By = today + 2 days, then authorizes if MXI actually asks and issues.
  - **Built from live ground truth, not just the recording.** A read-only diagnostic against the recording's own finished order (P000BDTA) confirmed the exact end state: line 1 price 0.00 / SCRAP / CR9REPAIR, line 2 price 175.00 / SCRAP / CR9REPAIR. It also confirmed the gap the user flagged — that order's ORIGINAL line still carries a stale 09-SEP-2026 promised-by while the new line got 25-AUG-2026 (recording date + 2), because the recording only ever set the new one. Both are now set explicitly rather than relying on any MXI default.
  - The charge-to-account is **read from the original line** before anything is added, rather than hardcoded — the recording typed CR7REPAIR then corrected it to CR9REPAIR to match. Reading first means there is never a moment where a wrong account is committed.
  - Resolved the recording's third opaque `{AES}` token the same way as the exchange one: `#idSelect2`'s option VALUES are AES blobs but its LABELS are clean (`SCRAP`), so it is selected by label. Separately confirmed that `#idPriceType_n` on Edit Lines uses plain values, so no token is involved there at all.
  - Refuses to act on an order that already has more than one line, rather than guessing which to zero. Independently re-verifies both prices and both types afterward.
  - **Not yet run against a real order.** CLI requires `--confirm`; first use should be one order, watched.

- **Exchange conversion in the Vendor Quote Writer.** Per explicit user direction: some quotes offer a replacement unit rather than a repair, and those orders must be converted via "Convert Repair To Exchange" instead of getting a normal price/ESD line.
  - The extractor now returns `suggestsExchange` + verbatim `exchangeEvidence`. The prompt draws an explicit line that matters: a vendor repairing our part and quoting for replacement PARTS consumed in that repair is NOT an exchange — an exchange means we get a different unit back. A wrongly-flagged exchange converts a real order to the wrong type.
  - New `convertRepairToExchange()` follows `discovery-Exchange-recording.ts`, then continues past where the recording stops (password + Issue Order, per user direction), and independently re-verifies that the order Type genuinely re-reads as EXCHANGE. It runs INSTEAD OF the price-line write, never alongside — the conversion form carries its own exchange price, so doing both would push the same money twice through two mechanisms.
  - **The recording's opaque `{AES}` reason token was resolved to a real label by live read-only diagnostic** (order P000BET4, production): the four real options are CONTRACT / OFFER / TIME / WARRANTY, and the recorded token matches **"OFFER (Vendor Exchange Offer)"** — correct by meaning as well as by token. Selected by label rather than token: the token proved identical across two different orders so it is probably stable, but "probably stable opaque blob" is not worth betting a real order type on. Same technique this project used for the Terms/Transport/Auth Flow dropdowns.
  - Guard 3 no longer requires a serial number (that requirement only existed to feed the now-removed cross-check), and exchange rows are exempt from the ESD requirement since the conversion form has no promised-by field.
  - Surfaced in the UI as an EXCHANGE badge with the vendor's own wording, so the different path is visible before anyone hits Write.

### Fixed
- **Move to Dock reported success without ever checking it happened.** User-reported: "a significant number of orders are being written up and issued, but the move to dock is not actually occurring, even though the UI says it is." `moveOutboundShipmentToDock()` returned `status:'success'` purely because nothing threw — it never re-read the dock state afterward. Every other MXI write in this project independently re-verifies its real outcome; this one was the gap, and "no exception" is precisely the signal that has already proven untrustworthy against MXI elsewhere (see the YES-timeout fix on 2026-08-21).
  - Now re-reads the real dock state from a fresh navigation after the click sequence, and reports success **only** when the shipment genuinely shows docked. A completed-but-unverified move is now an explicit failure naming the state it actually found, instead of a false success.
  - Also handles the shipment-line checkbox properly: `.check()` is strict-mode and throws on a multi-match, so each line is now checked explicitly rather than that surfacing as an opaque error. Zero selectable lines is its own named failure.
  - **Scale of the existing backlog: 788 rows are recorded `success` under the old, weaker rule**, so an unknown subset name orders whose part never moved. New read-only `npm run dock:audit -- [--limit N]` re-checks those against real MXI and prints the ones still sitting at USSTG. Clicks nothing and writes nothing — deciding what to do about a stuck order stays a human call. Bounded by `--limit` (default 25) because each order costs several real page navigations.

### Changed
- **Serial-number cross-check removed from the price-line writer, per explicit user direction.** It read as a strong failsafe, but PSA's internal BN system means our serial routinely and legitimately differs from the vendor's, so it rejected correct writes far more often than it caught real problems. The serial is still read and still recorded on every row — only the veto is gone, so a genuine mismatch stays auditable after the fact. Order number remains the identifying key, and the multi-line guard is untouched. Affects both the Invoice Price Writer and the Vendor Quote Writer, which share this function. `skipped_serial_mismatch` is kept in the outcome union, marked historical-only, because real rows already carry that value.

## 2026-08-21

### Added
- **Vendor Quote Writer — Stage 1 (Outlook ingestion) complete and proven.** New fourth workstream: read vendor quote PDFs out of a dedicated Outlook folder, extract them with Claude, review, and write price + ESD into MXI. Full design in `docs/VENDOR_QUOTE_WRITER_SPEC.md`, written before implementation per convention.
  - **Biggest risk retired first**: Outlook COM automation was probed for real on this machine before anything was designed — `Outlook.Application.16`, version `16.0.0.20228`, MAPI namespace OK, full folder tree enumerable. Had this been blocked by policy, the whole approach would have had to move to Microsoft Graph (Azure AD app registration + IT admin consent). It isn't, so there's no IT dependency and no stored mail credentials.
  - `scripts/read-outlook-quotes.ps1` — resolves one configured folder, saves PDF attachments, emits a single JSON envelope. **Read-only by construction**: contains no `Move`/`Delete`/`Save`/`UnRead` assignment at all, rather than a flag that could be flipped. A missing/misnamed folder fails loudly naming the missing segment and listing what folders *do* exist — never a silent zero-result that looks like "no new quotes."
  - `scripts/mark-outlook-mail-read.ps1` — the ONE mailbox mutation, deliberately isolated in its own script, single-message-by-EntryID only, idempotent, and independently re-reads to confirm rather than trusting the assignment.
  - `src/quoteWriter/outlookReader.ts` — `execFile` with an argument array and no shell (folder names contain spaces; this repo has been bitten by Windows shell quoting before). Surfaces the script's own specific error through a typed `OutlookReadError` instead of execFile's generic "Command failed."
  - **Verified against real mail**: real folder resolved (9,293 items), 7 real PDFs saved with correct sizes and collision-safe names, bad-folder error path confirmed, and — checked twice, including after the whole session — **every touched message still `UnRead=True`**. Gitignore proven via `npm run verify-gitignore` *before* any confidential PDF hit disk (`data/quote-attachments/` + `*.pdf` added, checked-path list extended); `git status` confirms zero PDFs visible to git.
  - **Real findings from the sample that shaped the design**: PSA order numbers appear in the email *subject* (`Repair Quote for your RO# P000BDY1`) and sometimes the *filename*, not just the PDF body — so subject and filename are passed to the extractor as context. One email legitimately carries several PDFs where only one is the quote (a `Shop-Finding-Report` alongside a `Repair-Quotation`), and non-quotes (a Receiving Discrepancy) do land in these folders — so quote-vs-not classification is required, not optional.
  - `src/quoteWriter/anthropicQuoteProvider.ts` written (Sonnet rather than the ESD path's Haiku — this one reads money off possibly-scanned documents, so accuracy outweighs per-call cost; forced tool-calling, one retry, safe explicit-failure result). **Stage 2 is NOT yet validated**: the `ANTHROPIC_API_KEY` in `.env` is returning `401 authentication_error`. The key is well-formed (108 chars, correct `sk-ant-api03-` prefix, no stray whitespace), so it appears genuinely expired or revoked rather than malformed. Extraction cannot be proven against real PDFs until it's replaced.

### Fixed (later same day) — the "YES" timeout, and the misdiagnosed reissueOrder anomaly
- **`confirmEsdLineEdit()` waited unconditionally for a "YES" link that only sometimes exists.** User-reported with the verbatim error `locator.click: Timeout 30000ms exceeded — waiting for getByRole('link', { name: 'YES' })`. That YES is the *"this line will need to be re-issued"* warning, which MXI only raises when the edit genuinely forces re-issue/re-authorization; on an order that doesn't need it, the OK commits and the page returns straight to RO Details, so the code waited 30s for a dialog that was never coming and then threw — **on a write that had already succeeded**.
  - **This resolves the long-documented "reissueOrder() reliability gap"**, which was a misdiagnosis. A real 40-order run had recorded thrown exceptions where the ESD nonetheless committed correctly, and that was attributed to `reissueOrder()`. It was actually this, one step earlier. `docs/PHASE2_MXI_WRITER_SPEC.md` now carries a correction above the original addendum (kept unedited — its observations were accurate, only the attribution was wrong).
  - Now waits for a **definitive end state** — YES present (click it), or already back on RO Details (nothing to confirm) — rather than assuming the warning always appears. Same content-aware-wait discipline as `waitForUnassignedTasksSectionResolved`, and the same conditional-YES precedent that `writeUps/shared/authFlow.ts` had been using all along but which was never applied here.
  - Deliberately does not throw when neither marker resolves: every caller independently re-verifies the real committed value, so a slow page becomes a verification failure with evidence rather than a spurious timeout on a working write.
  - The identical unconditional-wait shape in `reissueOrder()`'s trailing "OK" got the same treatment.
  - **Affects three features**, all of which share this selector: Invoice Price Writer, Vendor Quote Writer, and the ESD writer (`writeEsdAndNotes`). Whether the re-issue warning appeared is now recorded per row in the price writers' `issueDetail`.

### Fixed (later same day) — "the BER/X buttons and the write button do nothing"
- **Root cause was a stale backend process, not a UI bug.** User-reported: the BER and X buttons appeared inert, and the write confirmation dialog did nothing on Yes. Diagnosed by probing the *running* server rather than reading code: `/api/quotes/active-job` returned 401 (route present) while `/api/quotes/write` and `/api/quotes/extractions/:id/disposition` returned **404** — the server process had been started at 14:45, before those two routes existed. Reads worked because their routes predated it. Restarted; all three now return 401 (present and session-gated).
- **The real defect this exposed is that a failed request was invisible.** Both handlers already caught the error and set a banner — but the banner renders at the top of the page while the row-level BER/X buttons sit far down a long table, so a genuine 404 looked exactly like a button that did nothing. Two fixes: errors now scroll themselves into view (and are dismissable, with an icon), and a 404 on one of this tab's own endpoints reports the actual likely cause — "the API server is running an older build than this page — restart it" — instead of a bare `Request failed (404)`. Paths here are hardcoded and proxied same-origin, so a 404 essentially never means "wrong URL"; it means version skew.
- No change was needed to the disposition or write logic itself, which was correct.

### Added (later same day) — Vendor Quote Writer stage 5: the MXI write
- **Approved quotes now write to MXI**: Unit Price + Price Type=`QUOTE` + Promise By set to the ESD derived from the vendor's own quote — then, and only then, the source email is marked read.
  - **Reuses `writePriceLineUpdate()` rather than reimplementing the write.** That function is already live-proven and carries the serial-number cross-check, the integer-cents price comparison, and the always-re-verify discipline. It gained one optional `promiseByDate` parameter — **omitted, its behavior is byte-for-byte what the Invoice Price Writer already did** (tomorrow), so adding this second caller changes nothing about the live first one.
  - **Four structural guards in `quoteWriteRunner.ts`, none depending on the caller behaving**: (1) effective disposition is re-read from the DB, so an NREP/BER/excluded row cannot be written even if its id is passed explicitly; (2) `quoteExtractionAlreadyWritten()` blocks a second successful write, since re-writing reissues the order again for nothing; (3) rows missing an order number, price, serial, or ESD are skipped — no partial writes; (4) mail is marked read only after a verified-successful write, so a failure leaves the item in the unread queue rather than silently losing it.
  - A successful write whose *email* couldn't be marked read is reported as a **success with a footnote, not a failure** — the price and ESD really did land, and calling it failed would invite a "retry" of a write that already happened. `quote_writes.write_status` and `quote_writes.marked_read` are separate columns for exactly this reason.
  - New `POST /api/quotes/write` (session-gated, `env` required and never defaulted) plus a `Write N to MXI` button, environment selector, confirmation dialog, and per-row write-status column in the UI. New `npm run quote:write` CLI (`list` shows what is and isn't writable and why; `write` needs `--confirm`) specifically so the first real write against an environment can be done deliberately, one order, watched from a terminal.
  - **Verified**: endpoints registered and 401 unauthenticated; the `list` view correctly flipped two rows to blocked once marked BER and excluded, and a `write` explicitly naming a BER order **refused the whole request**; and the mark-as-read mutation was round-tripped for real against a live message (flipped unread, script marked it read, independently re-read to confirm, second call correctly reported `alreadyRead` — message left in its exact original state, net zero change to the mailbox).
  - **NOT yet exercised: a real MXI write.** Every guard around it is tested, but no price or ESD has actually been pushed to stage or production by this path. Per this project's standing discipline for a new kind of production action, the first one should be a single order, watched.

### Added (later same day) — Vendor Quote Writer: NREP / BER / exclude dispositions
- **Three distinct quote dispositions, deliberately not collapsed into one boolean** — they have different origins and different authorities. `excluded_nrep`: the VENDOR said the part is non-repairable, read off their document and applied automatically. `excluded_ber`: the CRA judged it Beyond Economical Repair — a PSA-side commercial call only a human may make. `excluded_other`: a plain "not this one" X, no reason required or recorded. Both scrap reasons are what a future scrap workflow will key on; `excluded_other` deliberately is not, since "skip this" says nothing about the part's condition. **The scrap process itself is explicitly out of scope** — this only records the signal.
  - NREP auto-excludes from the MXI write per user direction (no repair means no repair to price), but it is a default, not a lock: an analyst who disagrees with the model's read can set the row back to `pending`, recorded as their decision.
  - Human decisions live in a new append-only `quote_dispositions` table (latest row wins), same shape as `mxi_writes`/`write_up_issue_decisions`, so who decided what and when stays auditable rather than overwritten. The vendor-derived NREP flag lives on `quote_extractions` instead, because it's an extraction fact, not a decision. `POST /api/quotes/extractions/:id/disposition` **refuses `excluded_nrep`** — letting a client assert it would fabricate a vendor claim that was never made.
  - **REAL GAP FOUND AND CLOSED**: the extractor read only the PDF, subject, and filename, but a genuine message in the live folder states a scrap decision *only in the email body* ("The scrap fee for P000BCSG ... is $360") with nothing about it in the attachment. The body is now real extraction input (truncated to 6000 chars server-side). This matches the user's own framing — non-repairable arrives "in the email **or** the quote."
  - **Detection validated 5/5 on a controlled positive/negative suite** against a real quote PDF with varying bodies, including the two cases that matter most: an *expensive but repairable* quote is correctly NOT flagged (BER must stay a human judgement, never inferred from a big number), and a scrap fee quoted for a *different* order in a reply chain does not leak onto the attached quote. Both positives correctly produced `excluded_nrep` → not writable. Confirmed independently against real mail too: `P000B8V5` was correctly left writable despite "scrap" appearing in its body, because that text referred to other orders.
  - **Known extraction variability, stated plainly**: across five real runs of the same PDFs, prices were perfectly stable (`2157`, `196`, `3116.35` identical every time), but `P000BEGR`'s completion date came back `2026-08-20` on three runs and `null` on a fourth, shifting its derived ESD by 5 days. Not blocking — every row is human-reviewed before any write — but real, and worth knowing before trusting a date unattended.

### Added (later same day) — Vendor Quote Writer stages 3-4
- **`npm run quote:ingest` CLI + the tab is now genuinely wired into the UI.** The `/email-quotes` route previously rendered `EmailQuoteAnalysis.tsx`, a mock-data scaffold from the original UI shell whose own placeholder text read *"Full email body preview will render here once email integration is connected."* That page is now replaced by the real `VendorQuotes.tsx` — same route, same nav entry, no duplicate tab added. The mock page was deleted (its `emailQuotes` mock data had no other consumer, checked by grep).
  - New `quote_runs` / `quote_extractions` / `quote_writes` tables, same append-only shape as `runs`/`esd_inferences`/`mxi_writes`, plus `quoteExtractionAlreadyWritten()` — the same structural retry guard `esdWriteRunner.ts` uses, since writing a price/ESD twice reissues the order again and is not harmless.
  - `api/quoteWriter/quoteJobManager.ts` — a fourth independent `activeRunId`, separate from Order Write-Ups, ESD Finder, and Invoice Price Writer. Reuses `spawnRunner` for process discipline. Unlike the other three tabs there is no file upload to stage: the input is an Outlook folder read on the server's own machine. `api/jobRunners/quoteIngestRunner.ts` streams one envelope per PDF so the table fills progressively (extraction is ~7s per PDF).
  - Endpoints `POST /api/quotes/ingest`, `GET /api/quotes/active-job`, `GET /api/quotes/runs/:runId`, `POST /api/quotes/runs/:runId/cancel`, all `requireSession`-gated — **verified live returning 401 unauthenticated**, matching the existing known-good `/api/invoice-price/active-job`. `maxMessages` is clamped server-side to 1..100 regardless of what the client sends, because each PDF is a real paid model call.
  - `--unread-only` / an "Unread only" checkbox (default ON). Pairs with the post-write mark-as-read to make the folder a self-draining queue: unread == not yet processed. Uses Outlook's own `Restrict("[Unread] = true")` rather than an in-loop skip — with `MaxMessages` capping the walk, an in-loop skip would burn the whole cap on already-read mail and silently return fewer new quotes than asked for. **Verified against the real folder**: the filter's count agrees exactly with Outlook's own `UnReadItemCount` property (both 0 — all 579 existing quotes are already read, so unread-only correctly returns nothing today and will pick up new arrivals).
  - `DryRunQuoteProvider` runs the whole pipeline with zero API calls and no key; it still recovers order numbers from subject/filename by regex, so a dry run usefully shows which real orders a run *would* touch.
  - **Still not built: the MXI write (Stage 5).** Nothing in this tab writes to MXI or modifies the mailbox yet.

### Fixed
- **Notes to Receiver now states the vendor's own assumed ESD, not the buffered promised-by date.** Per explicit user direction — the original instruction ("state the pushed ESD") was a misunderstanding on the user's part, corrected here. The note used to repeat `inferred_esd`, i.e. the exact padded date written into the MXI ESD field; humans reading the note want the date the *vendor* actually gave. `assembleNoteText()`'s second parameter is now `extracted_base_date` (pre-buffer) instead. **The ESD field write itself is deliberately unchanged** — for a vendor-stated 8/24 the field still gets 31-AUG-2026 (+`SHIPPING_BUFFER_DAYS`), only the note now reads `ESD: 24-AUG-2026`.
  - Reads the stored base date rather than subtracting a constant: the user described this as "promised-by minus 7, or minus 14 for a quote date," which holds for `explicit_date`/`vendor_quote_estimate`, but `parts_pending` rows use an AI-chosen 20-30 day offset — subtracting a fixed 7 or 14 would have silently produced a wrong date on those. Verified all three classifications plus edge cases.
  - Updated all four real call sites (`server.ts`'s `/approve`, `cli/approveAndWrite.ts`, `jobRunners/esdWriteRunner.ts`, and `jobRunners/esdCompareRunner.ts`'s UI preview — the last matters because a preview that disagrees with the write is worse than no preview). `esdWriteRunner`/`approveAndWrite` needed `extracted_base_date` added to their SELECTs.
  - **Latent format inconsistency found and fixed while doing this**: `inferred_esd` is always normalized ISO (`formatISO`), but `extracted_base_date` on Step 2/3 rows was stored as the *raw* string the AI returned — harmless while it only fed the audit trail, but not safe once formatted for a real write. `applyInferenceRules.ts` now normalizes it on write, and `assembleNoteText` parses with the tolerant `parseFlexibleDate` (not `toMxiDateFormat`'s strict `parseISO`) so rows written before this change still format correctly. An unparseable value logs a warning and writes the note without the ESD segment rather than throwing and losing the vendor commentary too.
  - `tsc --noEmit` clean. **Not yet exercised against a real live MXI write** — worth watching the first real order through.

## 2026-08-20

### Fixed
- **Unassigned-task ignore list (PC/PC-PC/FORECAST/REPL) now actually lets the write-up continue, in both write-up engines.** Real gap found in Aero Repair's own `writeUp.ts`: when every task on the Unassigned Tasks line filtered out as an ignored type (nothing genuinely assignable left), it stopped and flagged the line for manual review (`unassigned_task_detection_suspect`) instead of proceeding — the opposite of the ignore list's own purpose. Fixed to continue the write-up exactly like the confirmed-absent state. Separately, the shared vendor-code engine (`vendorCodeWriteUp.ts` — 0T1Y4, VC01059, and the rest of that family) had no ignore-list filtering at all; any non-empty text on that sub-tab blocked the line (`unassigned_task_present`), including pure PC/PC-PC/FORECAST/REPL rows. Now reads the same row-level task types before blocking — deliberately does NOT add Aero Repair's auto-assign-the-remaining-candidate behavior to this engine, since that's a separate, unproven-at-this-scale capability across many already-live vendors and was never asked for.
  - The ignore-list mechanism (`UNASSIGNED_TASK_IGNORED_TYPES`, `readUnassignedTaskCandidates`, `assignUnassignedTask`) moved from `aeroRepair/unassignedTaskAssignment.ts` to `shared/unassignedTasks.ts` so both engines use one implementation (Aero Repair re-exports the old path for compat, zero call-site changes). Real DOM evidence supplied by the user (a live row's outer HTML) confirmed the per-row `id` attribute is not stable across lines, but `class="shortString"` on the task-type `<td>` is — the row-candidate read is now scoped to `td.shortString` specifically instead of scanning every `<td>` in the row.
  - `tsc --noEmit` clean across the whole backend.

## 2026-08-19

### Changed
- **Preferred-vendor check disabled globally, per explicit user direction — not removed.** The check (added 2026-08-18, see below) only behaves correctly for a specific list of part numbers that doesn't currently exist, so it's being disabled rather than trusted broadly for now. Single kill-switch (`PREFERRED_VENDOR_CHECK_GLOBALLY_ENABLED = false`, `writeUps/shared/vendorCodeWriteUp.ts`) at the one real decision point in `runVendorCodeWriteUp` — every other piece of the feature stays fully intact: `readPreferredVendorIndicator()` (`removalTaskInfo.ts`) still runs unconditionally and harmlessly per candidate (cheap, read-only), and the `vendor_not_preferred` outcome type plus its DB/UI mapping (`vendorCodeOutcomeLogging.ts`, `runLog.ts`, `db.ts`) are just dormant now since nothing returns that status while the switch is off. Deliberately a single global switch rather than a per-vendor edit across the registry — re-enabling later is one line, not re-touching every vendor config. Once the real part-number list exists, worth revisiting whether `VendorConfig.checkPreferredVendor`'s existing per-vendor override (see its own docstring, added in the same 2026-08-18 work) is a better fit than this all-or-nothing switch. `tsc --noEmit` / `npm run build` both clean.

## 2026-08-18

### Added
- **Invoice Price Writer — new, independent feature (own job slot, third workstream alongside Order Write-Ups and the ESD Finder).** Upload one billing/invoice Excel export, and for each row in its `Template` sheet (`PO Number` / `Serial Number` / `Extended Amt` columns), update the corresponding MXI order's Unit Price, set Price Type to `QUOTE`, reset Promised By to tomorrow (these orders are often already received; the date no longer matters, just needs to not be stale), and push through re-authorization (only when MXI's own page state shows it's needed) and reissue.
  - Built from two real `npx playwright codegen` recordings against **production** MXI, cross-checked against each other to separate real required steps from exploratory recording noise (confirmed directly with the user rather than assumed). One live, read-only diagnostic against real production order `P000BB8K` confirmed the serial-number selector (the `"(PN: X, SN: Y)"` pattern already used elsewhere in this codebase) before any code was written.
  - New `mxiWriter/priceLineSelectors.ts` (Unit Price / Price Type fields, serial-number extraction, real-page-state authorization-needed detection, the credential re-entry dialog) and `mxiWriter/writePriceLineUpdate.ts` (the combined orchestrator — serial-number cross-check happens **before any mutation**, skip immediately on mismatch, then price/type/date/confirm/reauth-if-needed/reissue, then independent re-verification, mirroring `writeEsdAndNotes()`'s discipline). Reuses `findOrderByNumber`/`updateEsdField`/`confirmEsdLineEdit`/`reissueOrder` from the existing `selectors.ts` unchanged.
  - New `invoice_price_runs`/`invoice_price_writes` tables (`db.ts`), same append-only two-table pattern as `runs`/`esd_inferences`. New `api/invoicePriceWriter/` (single-sheet ingestion + its own job manager) and job runner. New `server.ts` endpoints under `/api/invoice-price/*`, `requireSession`-gated. New frontend page (single-file drop zone, no multi-upload, live per-line results: order/serial/original price/new price/status) + nav entry.
  - **Real bug found and fixed the same day, user-reported from a live run**: MXI pads Unit Price to two decimal places on re-read (e.g. `"941.70"`), but the sheet's own Extended Amt cell often has fewer (`"941.7"`) — the verification step's plain string comparison flagged these as failures even though the write had genuinely succeeded. Fixed to compare numerically in integer cents instead of raw string/float equality.
  - **Retry for failed lines added the same day**, per explicit user request (MXI/Playwright timeouts are a normal, expected occurrence at scale). The originally-uploaded sheet is already deleted by retry time, so retry reconstructs each row's (serial number, new price) from the run's own append-only write history (`db.ts`'s new `getInvoicePriceRetryRows` — verified directly against a real SQLite DB, including that an order which already succeeded is never re-attempted even if requested, and that a row retried more than once reconstructs from its latest attempt) rather than needing the original file. Retries append to the SAME run (same job runId) rather than starting a fresh one — `invoicePriceJobManager.ts` now UPSERTS `order-result` envelopes into `job.results` instead of always pushing, so the results table shows the merged picture. New `POST /api/invoice-price/runs/:runId/retry`, validating requested order numbers against the run's own stored `'failed'` results before retrying. Frontend: a "Retry N failed" button next to "Start another run."
  - `tsc --noEmit` / `npm run build` both clean throughout. **Not yet live-tested against real MXI at the time this entry was written** — see `docs/INVOICE_PRICE_WRITER_HANDOFF.md` for the exact interrupted-test state and next steps.
- **Order writer: Create Work Package, expanded task-ignore list, preferred-vendor check** (commit `1fda13d`) — three additions to the Aero Repair / shared vendor-code write-up flows. *This entry is being added retroactively; the session that did this work didn't update this changelog at the time, so the description below is reconstructed from that commit's own message rather than written contemporaneously.*
  - **Create Work Package (Addition 1)**: replaces the old "No Work Package (Bad From Stock)" terminal exception. A line with no work package now gets one created (both the inventory checkbox and vendor radio confirmed checked first), named from parsed row data, then independently re-verified before continuing into the existing no-task/Ad-Hoc recovery flow. Gated by a one-time per-env proof (`workPackageCreationProof.ts`, mirrors the existing Ad-Hoc-continuation gate) before it runs unattended, per this project's standing discipline (#8). New CLI: `aero-repair:continue-work-package`.
  - **Expanded unassigned-task ignore list (Addition 2)**: the existing PC (Parts Card) filter is now a configurable list (`UNASSIGNED_TASK_IGNORED_TYPES`) also covering FORECAST, REPL, and PC-PC, exact-match only.
  - **Preferred-vendor check (Addition 3)** — see 2026-08-19 above; **disabled the day after this was added**, per explicit user direction, since it only behaves correctly for a part-number list that doesn't currently exist. Reads the read-only `td.checkbox > input[type=CHECKBOX]` indicator on each candidate's own row before any write; skips the bid (`vendor_not_preferred`, a normal outcome, not an error) when another vendor is preferred. Applied to every vendor in the shared vendor-code engine except Aero Repair, gated by `VendorConfig.checkPreferredVendor` for future narrowing.
  - New outcomes (`work_package_created`, `work_package_created_pending_manual_continuation`, `vendor_not_preferred`) threaded through `db.ts`, `processLine.ts`, the CLI outcome loggers, and the frontend-facing `runLog.ts` event mapper. `tsc --noEmit` clean across the whole backend, per the original commit message — no further verification detail available since this entry is reconstructed, not contemporaneous.

### Changed
- **ESD writer: wider date-push buffers, new note-only reissue path.** Per explicit user direction, cross-checked against the real code before building anything (two real discrepancies between the originating prompt and actual current state found and resolved with the user): `SHIPPING_BUFFER_DAYS` 3→7, `QUOTE_BUFFER_DAYS` 25→14. New `inference/classifyRowAction.ts`: rows with real vendor commentary but no usable ESD (`no_esd_found`) now get a note appended and the order reissued instead of being skipped outright, alongside the existing `esd_write` path and a `skipped_no_commentary` catch-all — the ESD field stays **structurally** guaranteed untouched on that branch. `assembleNoteText()` now states the pushed ESD inline when one exists. `EsdFinder.tsx` shows an "ESD Write" / "Note Only" badge per row. `tsc --noEmit`/build clean; not yet re-tested live against a fresh comparison run (see `docs/INVOICE_PRICE_WRITER_HANDOFF.md`).
- **Backend logging migrated from `console.*` to Pino.** Structured JSON (pretty-printed in dev only) across CLI tools, Order Write-Ups flows, MXI client/config, job manager, and diagnostics — 358 call sites, ~40 files. New `logging/logger.ts`. Job-runner-to-frontend JSON envelope streaming (`emit()` in each runner) confirmed untouched by design — a completely separate mechanism, never routed through Pino.

### Added
- **Local one-click launcher** (`Start-AMC-Repair-Suite.bat` + `scripts/prepare-local-env.cjs`) — double-click to install deps, prepare `backend/.env`, and start both servers, opening the browser once ready. **Real bug hit and fixed the same day**: the file was moved (not copied) from the repo root to the Desktop, breaking its own relative-path assumptions and producing confusing `npm`/`node` errors pointing at the wrong directory. Fixed with an early `package.json`-existence check (clear error instead of a confusing dump) and a real Windows shortcut on the Desktop pointing back at the file's real location, rather than a moved copy of the file itself.

## 2026-08-15

### Added
- **[#6-hardening][key-rotation] Key rotation / re-encryption tooling — Seam 2, per explicit user direction: eliminate the "lose `CREDENTIAL_ENCRYPTION_KEY` = every stored MXI password permanently undecryptable" risk, and build the rewrap mechanic the future Key Vault migration reuses.** Zero change to normal runtime behavior — a new offline utility + a backward-compatible ciphertext format, nothing about how the live app encrypts/decrypts today.
  - **Part A — version tag, not a `key_version` column.** Chosen over a schema column specifically because it's less invasive against the current format (`auth/crypto.ts`'s `iv:authTag:ciphertext`): zero `ALTER TABLE`, self-describing (can't drift the way a separate column could), and generalizes better to the Key Vault future (Key Vault has its own native secret versioning; an app-level AES-specific integer column wouldn't carry over). New format: `v<N>:iv:authTag:ciphertext` — `decryptSecret()` parses both the tagged and legacy untagged form transparently, so every existing row (implicitly "v1," no tag) keeps decrypting exactly as before, no forced migration. `encryptSecret()`'s default behavior (every existing call site — `authService.ts`'s `register`/`changePassword`) is **left byte-for-byte unchanged** — still emits the untagged format; only `rotateEncryptionKey.ts` ever writes a tagged value. `// NOTE:` comment added at that decision point: new writes stay untagged until Seam 3 tags `register`/`changePassword` with an explicit `v1` marker, since `authService.ts` will already be under edit there.
    - `crypto.ts`'s `encryptSecret`/`decryptSecret` both gained a small, additive, optional parameter (`keyOverride` for both; `version` to emit for encrypt) — verified exactly 3 existing call sites (all in `authService.ts`), all still calling with just the original single argument, reproducing today's exact output. New exports: `getCiphertextVersion(stored)` (rotation tool's own bookkeeping) and `parseEncryptionKeyHex(hex, varName)` (factored out of the existing 32-byte validation, reused for validating the OLD/NEW keys rather than duplicated).
  - **Part B — `backend/src/security/rotateEncryptionKey.ts`** (`npm run rotate-encryption-key [-- --dry-run]`), offline only, never imported by any request path. Reads `OLD_CREDENTIAL_ENCRYPTION_KEY`/`NEW_CREDENTIAL_ENCRYPTION_KEY` via `SecretProvider` (both names added to `secretProvider.ts`'s `KNOWN_SECRET_NAMES`, deliberately kept out of `.env.example`'s standing list — one-time rotation inputs, not normal config), never hardcoded or logged.
    - **Idempotency is decrypt-based, not tag-based, per explicit user direction on the original plan**: each row is checked against the **new** key first — if it already decrypts, the row is already on target and is left untouched (a stale or missing version tag can never cause an incorrect skip or an incorrect rewrap, only the real cryptographic outcome can). This is what makes re-running the exact same old/new pair a safe no-op instead of a second attempt that fails on every row.
    - **Real run**: unconditional backup of `auth.db` first (every time, no exceptions), then the entire row loop inside **one** `better-sqlite3` `db.transaction()` — all rows succeed and commit together, or the first row that fails to decrypt with both keys throws (auto-rollback, nothing written), reporting only that row's id/username. `--dry-run`: read-only, **scans every row in one complete pass and reports every single failing row**, not just the first — per explicit user direction, a dry run is meant to be a complete diagnostic, not stop at the first problem. Never prints key material, ciphertext, or decrypted plaintext in either mode.
    - **A real bug was found and fixed during live testing, not just typechecked**: the backup step originally used a plain `fs.copyFileSync`, which silently produced an **incomplete, stale backup** — `auth.db` runs in WAL mode (`authDb.ts`'s `journal_mode = WAL` pragma), so recent writes can live in a separate `auth.db-wal` file the main `.db` file doesn't yet have. Reproduced directly: a real row visible via the live SQLite connection was completely absent when the plain-copied file was reopened. Fixed with `better-sqlite3`'s own Online Backup API (`db.backup()`), which produces a single consistent snapshot that correctly includes WAL data.
    - **Verified live, against a disposable scratch copy of the real `auth.db`** (never the live file — every test pointed `AUTH_DB_PATH` at a scratch copy; the real file was independently re-checked afterward and confirmed untouched): dry-run correctly identified the one real row as needing rotation; a real rotation run correctly re-wrapped it, tagged it `v2`, and backed it up with the WAL fix confirmed working (backup row count matched the live source); round-trip correctness confirmed (decrypting the rotated row with the new key produced the exact same plaintext as decrypting the original with the old key — plaintext itself never printed, only a boolean match); confirmed the old key no longer decrypts the rotated row at all (genuine re-encryption, not just re-tagging); re-running the identical rotation reported 0 rewrapped / 1 already-on-new-key (true idempotency, not just claimed); a genuine dual-key-failure case (two random, wrong keys) cleanly aborted with the row's id/username named, exit code 1, and the source database independently re-verified afterward to still decrypt correctly with the real original key — nothing corrupted by the failed attempt.
  - **Part C — `security.md` updated** (not a new `backend/docs/key-rotation.md`, the alternative the user's own prompt offered — `backend/docs/` doesn't exist and doesn't match this project's established convention of specs in repo-root `docs/` and security-audience content in repo-root `security.md`/`privacy.md`): new §8 "Key rotation" (backing up the key, exact `--dry-run` then real-run invocation, what the summary means, what it never prints, the Key Vault forward-note), §1.3's operational note updated to point at it, §5's Known Limitations row flipped from "Not yet built" to "Built — see §8", §6's planned-work bullet struck through, and §7's compromised-key guidance rewritten — it previously said "there is currently no way to re-encrypt existing rows... without each user re-entering their password," which is no longer true.
  - `tsc --noEmit` / `tsc -b` both clean. Constraints honored: AES-256-GCM unchanged, scrypt/login path untouched, DB schema untouched (no new column, no migration), `.env` stays the real source of the currently-active key.
- **[#6-hardening][secrets-seam] Secrets abstraction — Seam 1 of N, per explicit user direction: make the eventual Azure Key Vault swap a config change, not a rewrite.** Pure refactor, zero behavior change (confirmed by `tsc --noEmit`/`tsc -b` both clean, and by design — see below). New `backend/src/security/secretProvider.ts`:
  - `SecretProvider` interface — `init(): Promise<void>` (async, loads once — Key Vault's own fetch will be async), `get(name: string): string` (stays synchronous, so existing call sites barely change).
  - `LocalEnvSecretProvider` — `init()` reads a fixed list of 9 known secret names from `process.env` into an in-memory `Map`; `get()` serves from that cache.
  - `getSecretProvider()` — singleton factory gated by `SECRET_PROVIDER` (default `'local'`), new var documented in `.env.example`. `// TODO: case 'keyvault'` stub left in place (not implemented), with a comment describing the Managed Identity / `DefaultAzureCredential` pattern, plus an explicit note that this project's spawned child processes (`discoveryRunner.ts`, `executeRunner.ts`, `esdWriteRunner.ts`, `esdCompareRunner.ts`, every CLI tool in `src/cli/`) each get their own `SecretProvider` instance and would independently authenticate to Key Vault — flagged as something to verify at actual Azure deployment time, not something local testing can confirm.
  - `getOptionalSecret(name)` — a thin wrapper (try `get()`, return `''` on throw), kept outside the `SecretProvider` interface itself. Exists specifically to preserve two call sites that today tolerate a missing secret rather than throwing (see the behavior-preservation note below) — not a second method every future provider has to implement.
- **A real design tension was found and resolved before writing any code, not discovered mid-edit**: the ask was both "init() validates each secret exists, throwing a clear startup error if one is missing" AND "zero behavior change." Checked every current call site directly rather than assuming: none of the 9 secrets throw at process boot today — `CREDENTIAL_ENCRYPTION_KEY`/`MXI_STAGE_*`/`MXI_PROD_*`/`ANTHROPIC_API_KEY` throw lazily, only when actually used; `MXI_USERNAME`/`MXI_PASSWORD` never throw at all (silent `?? ''` fallback in `mxiWriter/config.ts`); `AUTOMATION_API_KEY` is checked per-request inside `requireAutomationKey`, not at boot (a server missing it today still starts fine — only the 3 gated Power-Automate routes 500). Making `init()` eagerly validate and throw on any of these would break server startup for configurations that currently boot successfully — a real regression, not a refactor. **Resolved with explicit user confirmation**: `init()` loads whatever's present and does not throw for absent ones; `get()` throws instead, at the same moment (first actual use) a missing secret would have caused a problem before — centralizing each call site's own existing lazy-throw, not changing when/whether it fires. A future `strict` fail-fast mode is a good idea but a deliberate, separate, later change, not bundled into this one.
- **Every real secret call site found and routed** (grepped both dot-notation `process.env.X` and bracket-notation `process.env[...]` across `backend/src` — the bracket form in `cliMxiClient.ts` doesn't match a naive `process\.env\.` grep and was easy to miss):
  - `auth/crypto.ts`'s `loadEncryptionKey()` — `CREDENTIAL_ENCRYPTION_KEY`, via `getSecretProvider().get(...)`. The 32-byte length validation that follows is unchanged; only the missing-key check moved into the provider.
  - `mxiWriter/config.ts`'s `loadMxiConfig()` — `MXI_USERNAME`/`MXI_PASSWORD`, via `getOptionalSecret(...)` (preserves the old `?? ''`, never throws here — see the tension note above).
  - `mxiWriter/cliMxiClient.ts`'s `createReadyMxiClient()` — `MXI_STAGE_USERNAME`/`MXI_STAGE_PASSWORD`/`MXI_PROD_USERNAME`/`MXI_PROD_PASSWORD` (dynamic `${prefix}_USERNAME`/`${prefix}_PASSWORD`), via `provider.get(...)`. `${prefix}_BASE_URL` deliberately left as a plain `process.env` read — same-for-every-analyst public MXI URL, not a secret, per `.env.example`'s own existing comment. This function is the single chokepoint every CLI tool and job runner in the backend goes through for MXI credentials (confirmed by grep across `src/cli/` and `src/api/jobRunners/` — `mxiReadEsd`, `mxiWriteEsd`, `approveAndWrite`, `saveStorageState`, all 3 `vendorCode*Cli`, all 8 `aeroRepair*Cli`, `discoveryRunner`, `executeRunner`, `esdWriteRunner` all resolve through it), so `secretProvider.init()` lives here as one line rather than being repeated at ~18 separate entry points.
  - `server.ts`'s `requireAutomationKey` — `AUTOMATION_API_KEY`, via `getOptionalSecret(...)` (preserves the per-request 500-not-throw behavior). `server.ts`'s `main()` — added `await getSecretProvider().init()` as the first line (this process never goes through `cliMxiClient.ts`'s chokepoint for its own shared boot-time client, so it needs its own explicit call).
  - `api/jobRunners/esdCompareRunner.ts` — `ANTHROPIC_API_KEY`, via `secretProvider.get(...)` after its own explicit `init()` call (this runner never touches MXI, so it never reaches `cliMxiClient.ts`'s embedded init either). Not in the user's original named list, but clearly a secret — flagged as "anywhere else" and confirmed in scope.
- **Found and deliberately left as plain `process.env` reads, with reasoning, per explicit user confirmation**: `MXI_STAGE_BASE_URL`/`MXI_PROD_BASE_URL` (public URLs, not secrets), `MXI_ENV`/`HEADLESS` (mode/feature flags), `DEFAULT_APPROVED_BY`/`MXI_DB_PATH`/`AUTH_DB_PATH`/`PORT` (config, not credentials), `ESD_FINDER_DEBUG_HEADERS` (debug flag), and `jobManager.ts`'s `{ ...process.env, ...envOverrides }` spawn-env spread (forwards the whole environment to a child process — not a named secret read; the individual secrets it forwards are still read correctly by the child via the routed sites above).
- **`kpiDb/config.ts`'s `KPI_DB_SERVER`/`KPI_DB_NAME`/`KPI_DB_USER`/`KPI_DB_PASSWORD` deliberately deferred, not routed** — real secrets, but this whole module is dead code today (confirmed in `privacy.md`: "not currently wired into any running code path"), so routing it now would mean guessing at requirements for a feature that doesn't run. Per explicit user instruction, added a `// SECURITY TODO: route KPI_DB_* through secretProvider when wired live` comment directly at the reads in `getKpiDbConfig()` so this can't get silently activated on the old raw-`process.env` path later.
- Verified: `tsc --noEmit` and `tsc -b` both clean across the whole backend. `.env` stays the real source of every secret value today — nothing about where credentials are stored, how they're encrypted, or the DB schema changed.

## 2026-08-14

### Changed
- **[#1] Charge-to-account default rule relaxed, per explicit user instruction given right after the CR7HMV failures below were reported**: "the removal site" can leave a genuinely different value autofilled depending on where the part was removed — not just the exact `<CR-prefix>ROUTINE+NONROUTINE` shape `buildChargeToAccountWithSuffix` used to require. New rule: unless a vendor is explicitly stated otherwise, this suite always lands on `<CR-prefix>REPAIR`, defaulting the prefix to `CR7` if none is present at all — the only two exceptions are Aero Repair's `WHEELSBRAKES` flow (its own separate module, `aeroRepair/chargeToAccount.ts`, untouched) and Collins (76863's fixed `COLLINSDISPATCH100` suffix, which still goes through the original strict function since it's a genuinely different value, not "REPAIR" with a different prefix).
  - New `buildDefaultRepairChargeToAccount()` (`shared/chargeToAccount.ts`) — extracts just the leading `CR<digits>` prefix if present (no longer requires the full `ROUTINE+NONROUTINE` tail), defaults to `CR7` if none found, never throws. The original `buildChargeToAccountWithSuffix` is unchanged and still used for Collins.
  - `vendorCodeWriteUp.ts`'s charge-to-account branch now dispatches on `config.form.chargeToAccountSuffix === WARRANTY_TERMINAL_STATE_CHARGE_TO_ACCOUNT_SUFFIX` ('REPAIR') — true for every vendor in the shared engine except 76863 — rather than always calling the strict function.
  - **Re-verified for real against the exact 4 lines that failed earlier the same day** (see the new-vendor-batch stage-testing note below): all 4 now succeed — `CR7HMV` correctly resolves to `CR7REPAIR` on real orders `P000BE7C`, `P000BE7D`, `P000BE7E`, `P000BE7F`. `tsc --noEmit` clean.

### Added
- **[#1] "Email Maintenance Records" draft button on zero-usage exception lines**, per explicit user instruction with an exact requested blurb. A `zero_usage` line (times/cycles read as all-zero — a records-team problem, not a write-up problem) now gets a button, right next to the existing "Show technical details" toggle on that same line, that opens a pre-filled email draft: fixed blurb ("Good morning Maintenance Records team!...") plus the real times/cycles table for that specific part. Recipient `DL_PSA_MaintenanceRecords@psaairlines.com`; subject `Zero Times & Cycles — PN <partNumber> / SN <serialNumber>`.
  - **Two real design questions resolved with the user before building**: (1) mechanism — a `mailto:` link (opens whatever's registered as the OS default mail handler, pre-filled; user reviews and hits Send themselves, nothing auto-sent, same "never automatic" discipline as every other action in this project) **plus a Copy-draft button right next to it**, since a mailto: link does nothing on a machine with no registered handler (a real, plausible gap in a corporate Office365/Outlook-web environment — not a security block, confirmed and explained to the user, who then asked for the belt-and-suspenders version rather than relying on the link alone); (2) scope — covers zero-usage exceptions from BOTH write-up flows (the shared vendor-code engine's 0T1Y4/Barfield family + new 26-vendor batch, AND Aero Repair), not just one.
  - **Real gap found and fixed to make this possible**: the actual times/cycles table (`UsageParmRow[]`, already read live during every write-up via `shared/partOwnDetails.ts`) was never threaded any further than the immediate zero-usage check in either flow — both `zero_usage` outcomes (`shared/vendorCodeWriteUp.ts` and `aeroRepair/writeUp.ts`) carried only `partNumber`/`serialNumber` (Aero Repair's own `ProcessLineResult` variant carried neither). Added `usageRows` to both outcome types, persisted into `filledFieldsJson` on both flows' `zero_usage` DB inserts (previously just `{ serialNumber }`), and added a new structured `usageRows?: UsageParmRow[]` field on `RunLogEvent` (`api/runLog.ts` + frontend `lib/api.ts`, mirrored) — deliberately not folded into the existing free-text `detail` field, so the frontend builds a clean table instead of parsing one back out of prose.
  - Same plain-text `Usage Parm\tTSN\tTSO\tTSI` table shape already used in the real Notes to Vendor text elsewhere in this codebase (`composeNotesForNormalLine`) — reused, not reinvented.
  - Verified: `tsc --noEmit` clean (backend + frontend), `vite build` clean, `eslint .` clean. Not yet exercised against a real zero-usage line in the browser (no real zero-usage exception occurred during this session's own stage testing) — worth a quick look next time one comes up live.

### Added
- **Cancel button (ESD Finder + Order Write-Ups)** — a visible Cancel control during any active run (discovery/execute for Order Write-Ups; compare/write for ESD Finder), gated behind a confirmation dialog, plus persistent run state so navigating between tabs mid-run no longer loses progress or the cancel control. Real, end-to-end backend cancellation, not a frontend-only "stop watching":
  - **Cooperative cancellation, not process signals** (`backend/src/api/jobRunners/cancellationWatcher.ts`): this project runs on Windows, where `ChildProcess.kill()` force-terminates immediately regardless of signal argument — a runner mid-Playwright-session would have zero chance to call `client.shutdown()`, risking an orphaned headless Chromium process still holding a real MXI login. Instead, the parent writes a single `{"type":"cancel"}` JSON line to the runner's stdin (`spawnRunner`'s stdio changed from `['ignore','pipe','pipe']` to `['pipe','pipe','pipe']`); every runner that opens a real MxiClient (discovery, execute, esdWrite — not esdCompare, which never touches a browser) checks the resulting `AbortSignal` between lines/vendors/orders, never mid-action, so its own existing `finally { await client.shutdown() }` still runs on a normal early-return. `jobManager.ts`'s `requestCancellation()` still keeps a 10s grace-period hard-kill fallback for a runner that never reaches its own next checkpoint — accepted as a rare last resort, not the primary mechanism.
  - **New `'cancelled'` job status** (`JobStatus`/`EsdJobStatus`), distinct from `'failed'`. `cancelJob()`/`cancelEsdJob()` set it synchronously the moment cancellation is requested (so a poll immediately after already sees it, without waiting for the child to actually exit) and free the job slot immediately; the runner's own exit handler checks `cancelRequested` first, before fatalError/exit-code, so a runner that exits 0 after honoring a cancel is still recorded as cancelled, never completed.
  - **Two new endpoints**, same `requireSession` gate as every other browser-UI route: `POST /api/runs/:runId/cancel`, `POST /api/esd/runs/:runId/cancel`.
  - **Cancel semantics, per explicit user direction**: cancelling a *read* (discovery/compare) discards it entirely — nothing was written, nothing to preserve. Cancelling a *write* (execute/esdWrite) does NOT discard anything — lines/orders that already completed keep their real results untouched; the run returns to its review screen with only the not-yet-attempted lines re-offered, never the already-completed ones (a deliberate choice, confirmed with the user, over restoring the full original selection — re-offering an already-completed line would risk creating a duplicate real order).
  - **`targetLineCount`/`sourceCompareRunId` added to the job registries** (`jobManager.ts`'s `Job`, `esdFinderJobManager.ts`'s `EsdJob`) — real gaps found while building the "return to review after cancel" flow: neither the original target-line count nor (for ESD Finder writes) which compare run a write came from was previously exposed anywhere the frontend could recover after a fresh page reload mid-run, only from data the page itself happened to still hold in memory.
  - **Frontend**: `lib/executeRun.tsx` (execute-only) replaced by `lib/orderWriteUpsRun.tsx` (discovery + execute) and new `lib/esdFinderRun.tsx` (compare + write) — both mounted in `components/RequireAuth.tsx` (same reasoning as the original ExecuteRunProvider: only meaningful once a session exists). Both pages' line/order selection state was restructured from "the selected set" to "deselections from an implicit default-all-selected baseline, computed at render time" — the original design re-initialized selection via a `useEffect` on phase transitions, which `eslint-plugin-react-hooks`' `set-state-in-effect` rule correctly flagged (a setState directly inside an effect, even ref-guarded to fire once); computing "selected = eligible minus deselected" at render time needs no effect at all, and a new, smaller eligible set (e.g. after a cancel) is correctly all-selected for free. New shared `components/ConfirmDialog.tsx`, extracted from the same visual pattern already proven in `EnvironmentBar`'s inline "Switch to Production?" dialog (that original stays untouched, per the same "don't risk regressing an already-working confirm" reasoning that motivated pulling `EnvironmentBar` itself out earlier).
  - **Verified**: `tsc --noEmit` clean (backend + frontend build), `eslint` clean. Backend server boots cleanly with the new routes wired in and correctly gated (`POST /api/runs/:runId/cancel` and `POST /api/esd/runs/:runId/cancel` both return a real `401` unauthenticated, same as every other session-gated route) — checked against an isolated port, not the already-running instance on the default port, to avoid disturbing it. Frontend dev server boots clean with both new providers mounted. **Not verified**: an actual live run being cancelled mid-flight end-to-end — this session's own MXI access was still blocked by the Akamai issue noted below at the time, so this couldn't be exercised against a real discovery/execute/compare/write job. Recommend running one real cancel of each kind (a read and a write, on at least one of the two tabs) once MXI access is back, before trusting this in a busy production session.
- **[#1] New vendor batch: 26 vendors registered in `VENDOR_REGISTRY`** (`backend/src/writeUps/shared/vendorRegistry.ts`), all via `buildWarrantyTerminalStateVendorConfig` — confirmed by explicit user direction to be the same real process as the existing 0T1Y4 family (vendor-code search + BN-prefix override + warranty terminal state). **No per-vendor Playwright recording exists for this batch** — a deliberate, explicit departure from this project's usual "never guess a selector/value" discipline, at the user's own direction ("let live testing confirm"), since every prior vendor addition required a real codegen recording first. All 26 get `hasPartDetailsStep: true` (previously confirmed present only for 76863/1DH10) — a vendor that genuinely lacks the step will surface as a real, visible Playwright timeout the first time it runs, not a silent skip.
  - VC00814 AIRGROUP DYNAMICS INC, VC00584 AIRLINE COMPONENT PARTS LLC, 5YRM0 CAMTRONICS LLC, VC00569 CHAMPION AEROSPACE LLC, VC00870 CIRCOR AEROSPACE INC, 1BAY3 CSI AEROSPACE INC, VC00524 DCM GROUP INC, 8719 DUCOMMUN TECHONOLGIES, 2750 EATON CORPORATION, 59875 EATON INDUSTRIAL CORPORATION, VC00879 FIRSTMARK AEROSPACE CORPORATION, 1DH10 HRD AERO SYSTEMS INC, 58657 LEACH - CA, VC01014 LEADING EDGE AEROSPACE, VC01197 LIEBHERR AEROSPACE - NORTH MAPLE RD, 8S625 LIEBHERR AEROSPACE SALINE INC, 76227 LIEBHERR-AEROSPACE LINDENBERG GMBH, 0B9R9 MEGGITT AIRCRAFT BRAKING SYSTEMS, 0VXA1 MIDWEST AERO SUPPORT LLC, VC00445 REXNORD INDUSTRIES LLC, 75521 ROTRON INC., 16630 TAT-LIMCO, VC00399 THALES AVIONICS INC., 67107 TRIUMPH CONTROLS INC, 67365 WOODWARD INC, 19710 WOODWARD MPC.
  - **1DH10 and VC00399 have real supporting evidence**, independent of the "no recording" caveat above: both appear in real recordings reviewed earlier this same session (`discovery-7A9Y2-AJS--recording (4).ts`/`(5).ts`, from `Downloads/`) with direct-fill Charge To Account `CR7REPAIR` — matching this family's own default suffix exactly, no override needed for either.
  - **The 26-vendor list itself went through a real correction mid-session**: the user's first list used **certificate numbers, not real MXI vendor codes** (user's own words: "I made a big mistake here lol"); the list above is the corrected one. The two Liebherr entries are genuinely **two separate real vendors** (not a duplicate, an earlier working assumption made obsolete by the corrected codes) — `VC01197` (North Maple Rd) and `8S625` (Saline).
  - **DCM GROUP INC's code**: an earlier one-off correction from the user gave `VC005241`, but that correction was applied to the since-invalidated certificate-number list. The final corrected list (which the user explicitly called "the correct list") gives `VC00524` (no trailing "1") — used here. **User confirmed `VC00524` is correct** after this was flagged.
  - **RMA activated for 3 of the 26**: `RMA_VENDOR_IDS` (`backend/src/writeUps/shared/rmaVendors.ts`, previously empty scaffolding) now contains `8719` (Ducommun Techonolgies), `58657` (Leach - CA), `75521` (Rotron Inc.) — confirmed via explicit user direction after an initial round of confusion (the user's first list of "vendors needing RMA" also turned out to be certificate numbers for 3 of 5; cross-referenced against the corrected code list to recover the real matches). Two other codes the user initially named (`VC00664`, `0DLY8`) are real vendors but explicitly deferred — "different processes... skip today," not added to this batch. No VendorConfig-level change was needed for these 3 — `isRmaVendor()` is a pure vendor-code-membership check, independent of the registry entry itself.
  - **New outcome: `receiving_notes_flagged_account`** — per explicit user instruction ("If anything comes up in the part notes that reads in the word 'account', flag it and move on. I don't want wrong accounts"). `shared/vendorCodeWriteUp.ts` now checks the part-level receiving-notes text (already being read for every `hasPartDetailsStep` vendor, previously logged only) for a case-insensitive `"account"` substring; a match returns this new outcome — no order created, nothing filled — instead of proceeding to Schedule Work Package. Threaded through every exhaustive switch on the outcome type, same discipline as every prior outcome addition this project: `VendorCodeWriteUpOutcome` union, `vendorCodeOutcomeLogging.ts`, `api/runLog.ts`'s event mapper, `db/db.ts`'s (TS-level, free-text column) outcome union.
  - **Real stage testing, done same day.** Step 1 — read-only `npm run vendor:batch-discovery -- --env stage` across all 37 registered vendors (11 existing + 26 new): every one of the 26 new vendor-code searches navigated and resolved cleanly (`real_row` or genuine `no_inventory`, 0 indeterminate) — confirms the shared vendor-code-search mechanism itself works uniformly across the whole new batch, as the user asserted. 5 of the 26 had real candidate lines on stage that day: `1BAY3` (1), `1DH10` (2), `VC01014` (1), `8S625` (4), `0VXA1` (1). The 3 RMA vendors and `VC00524`/DCM had zero candidates that day — RMA path and DCM remain unexercised.
    - Step 2 — real write-up executed against all 9 of those lines (`npm run vendor:batch-execute -- --env stage`, scoped to just the new vendors' lines, not re-touching the already-proven family). Real, mixed results, all genuine MXI state rather than any config/selector bug found in this code:
      - **`1BAY3` and one `8S625` line fully succeeded end-to-end**: real orders `P000BE7A`/`P000BE7B`, `AUTHORIZATION_ONLY`, correct suffix-derived Charge To Account (`CR9ROUTINE+NONROUTINE` autofill → `CR9REPAIR`), `hasPartDetailsStep` ran cleanly on both (no receiving notes present on either real line, so the new "account" flag logic executed without error but was **not exercised against actual "account" text** — still unverified against a real matching case).
      - **`1DH10` (both lines) and `VC01014`**: correctly detected a genuine unassigned task already present on the real line and stopped (`unassigned_task_present`) — expected behavior, needs manual task assignment in MXI first, not a vendor-config issue.
      - **`8S625` (3 of 4 lines) and `0VXA1`**: all originally hit the strict guard — Charge To Account autofilled to the literal `CR7HMV` instead of the expected `<CR-prefix>ROUTINE+NONROUTINE` shape, correctly refusing to guess a replacement rather than risk writing to the wrong account. Reported to the user, who clarified this is expected/real (removal-site-dependent autofill, not an anomaly) and gave the general rule now implemented above ("Changed" section, this same date). **Re-run after that fix: all 4 now succeed** — see that entry for the real order numbers.
    - **Net read**: the shared engine, `hasPartDetailsStep`, and the new account-flag plumbing all behave correctly against genuinely new, unrecorded vendors. The one real gap the CR7HMV case surfaced (an overly strict charge-to-account shape requirement) is now fixed and re-verified. Still open: the account-flag logic itself has never fired against real matching text (no test line had "account" in its notes); the RMA path and DCM Group remain completely unexercised (zero real candidate lines for any of the 4 that day) — test again whenever real lines appear for those.

### Fixed
- **Real bug, user-reported and reproduced same-day: every run silently hung on the UI after headless was turned off to debug the Akamai issue.** Root cause traced directly, not guessed: `spawnRunner`'s stdio change above (`'ignore'` → `'pipe'` on stdin, needed so the parent can send a cancel message) meant `cancellationWatcher.ts`'s `readline.createInterface({ input: process.stdin })` now put a REAL pipe into flowing mode — an active handle that keeps a Node process alive in its own right, independent of whatever else the process is doing. A runner's real work could finish completely (real MXI login, real search, real results found, `client.shutdown()` genuinely closing the browser, a `'done'` envelope written to stdout) and the child process would still never exit, because this one open stdin listener alone kept the event loop alive waiting for input that was never coming. From `jobManager.ts`'s side (which only marks a job finished on the child's `close` event) this looked exactly like a hang — the run had actually succeeded, but nothing ever reported it. Reproduced directly: spawned `discoveryRunner.ts` exactly as `jobManager.ts` does, against a real vendor in production — stdout showed a clean `'done'` envelope with real found lines, but `close` never fired. Fixed with a single `process.stdin.unref()` right after creating the readline interface — tells Node this handle shouldn't count toward keeping the process alive, so the listener can still react to a real cancel message while other real work is in flight, but no longer blocks a normal exit once that work is done. Re-verified with the identical reproduction: clean `[exit] 0` after ~14s, matching normal run time.
- **Incidental, real, and separately significant**: this same debugging session confirmed turning off `HEADLESS` fixed real MXI login (previously failing with the Akamai "Access Denied" edge block noted earlier today) — strong evidence the block was specifically fingerprinting/rejecting headless Chromium traffic, not blocking this server's IP/traffic outright. Login now succeeds headed; the stdin-hang bug above was the next, previously-unreached failure once login started working again. `HEADLESS=false` is now set in `backend/.env` (temporary, for this debugging — flip back once done, since a permanently headed run leaves a visible browser window open on whatever machine runs the server).

### Investigated (no code changes — item already complete)
- **[#1]** Skypaxxx (7A9Y2) non-shipset case, the item deferred on 2026-08-12. User reported it "read in nothing" for non-shipset lines this morning. Investigated properly before writing any code:
  - Static review found no bug: `resolveShipsetCase()` already returns `null` (never throws) whenever a line's USSTG task name doesn't match the shipset trigger, falling through to `buildWarrantyTerminalStateVendorConfig`'s standard template — the same WARRANTY-default/BN-prefix-override-to-REPAIR flow every other vendor in the family uses. `findCandidateLinesForVendorCode` (discovery) is pure vendor-code search with zero shipset-awareness.
  - **Confirmed via the real audit trail, not assumed**: `data/audit.db` already has a real, successful non-shipset 7A9Y2 outcome from *this same day* — `write_up_actions` id 1105, part `41031390-501EL`, `outcome: 'issued_and_docked'`, `2026-08-13T12:38:36Z` — part of a larger real multi-vendor batch (`aeroRepair`, `10933`, `21844`, `0t1y4`, `7a9y2`) run 11:47-19:22 UTC that day, all succeeding. User confirmed this was their own run. **Conclusion: the non-shipset case was already fully working before this session started** — nothing to implement.
  - Reviewed real recordings in `Downloads/` (`discovery-7A9Y2-AJS--recording*.ts`) as independent confirmation: the one genuine non-shipset 7A9Y2 example (order `P000BD5Z`) manually searched/selected Charge To Account `CR9REPAIR` — exactly what the existing `<CR-prefix>` + `REPAIR` suffix-derivation already produces for Skypaxxx's baseline config. Also found, mixed into the same numbered files, real examples for OTHER not-yet-registered vendors (`75818`/HAM SUND-FL, `4X623`/Rockwell Collins-Wichita, `VC00399`, `1DH10`, `2N512`/Aerotron — the last already noted as deferred) — available for whenever that batch continues. Three of these (`VC00399`, `1DH10`, `2N512`) directly confirm Charge To Account accepts a plain `.click()+.fill()` on `#idEditFieldChargeToAccount`, not just the "Select Account" popup search shown in the Skypaxxx/76863 recordings — de-risking the existing direct-fill implementation. Purchasing Contact varied across recordings (`232275`, `238784`) — confirmed (again) to be the operator's own ID, not a vendor value; the shared family default (`717375`) is correct and untouched.
- **Real, unrelated infrastructure finding**: this session's own MXI login attempts (both stage and production) failed consistently with an Akamai edge "Access Denied" (`errors.edgesuite.net`) — not a login-selector bug. Confirmed via direct navigation: happens even hitting `ToDoList.jsp` directly (skipping `/security/login.jsp` entirely) and even with a saved session's cookies loaded (`data/mxi-production-storage-state.json`) — rules out both "it's the login page specifically" and "it's session/cookie state," pointing at an IP-reputation/bot-detection block at the Akamai layer instead. Given the real audit trail shows clean activity through 19:22 UTC the prior day with zero Access Denied entries, this block is very recent — plausibly triggered by this session's own burst of automated MXI traffic (an unrelated fix-verification task run immediately before this one). Not resolved this session; blocks any further live testing until it clears or is addressed on the network/IT side.
- ~~Real, unrelated recurring failure noticed while reviewing the audit trail... the underlying MXI data needs manual review~~ — **resolved, not a data problem.** `0t1y4` / part `MXP411-4` failed twice on 2026-08-13 with `Charge-to-account value "CR7HMV" did not match the expected "<CR-prefix>ROUTINE+NONROUTINE" shape`. The same shape hit 4 more real lines during this same day's new-vendor-batch stage testing (see below) — confirmed by the user to be expected, removal-site-dependent autofill, not stuck/anomalous data. Fixed via the charge-to-account default-rule change (see "Changed" above); a re-run of `0T1Y4`/`MXP411-4` would now be expected to succeed the same way the 4 re-tested lines did, though that specific line itself hasn't been re-run this session.

## 2026-08-13

### Changed
- **[#6]** Repository restructuring — the third and final piece of `#6`, executed as a reviewed proposal (see conversation) before any file was moved. `tsc --noEmit`, `tsc -b`, and `npm run verify-gitignore` all clean after every step; a few of the moved CLI scripts were actually re-run (`--help`/no-args), not just recompiled, to confirm they work post-move.
  - **`backend/src/cli/`** — all 15 standalone CLI entry points (`cli.ts`, `mxiReadEsd.ts`, `mxiWriteEsd.ts`, `approveAndWrite.ts`, the 8 `aeroRepair*Cli.ts` files, the 3 `vendorCode*Cli.ts` files) moved out of `backend/src/` root into their own folder — they were sitting flush alongside the organized module folders (`api/`, `auth/`, `db/`, etc.) with nothing distinguishing "one-shot CLI script" from "shared module." `server.ts` and `types.ts` stay at `src/` root — genuinely different kinds of things. Every moved file's internal relative imports updated (`./x` → `../x`, including one dynamic `import()` a static grep missed and `tsc` caught); `backend/package.json`'s 15 corresponding npm script *paths* updated to match — script *names* (`npm run cli`, `npm run aero-repair:write-up`, etc.) are unchanged.
  - **`docs/`** (repo root, new) — `ESD_FINDER_HANDOFF.md`, `VENDOR_MODULE_REFACTOR_SPEC.md`, and `backend/PHASE2_MXI_WRITER_SPEC.md` consolidated here. `README.md`, `CLAUDE.md`, `security.md`, `privacy.md`, `backend/CHANGELOG.md`/`backend/README.md` deliberately left at their conventional locations. Checked first for real markdown links to these 3 files (none existed — only prose mentions of the filename, which stay valid as text) and for any code importing the moved CLI files from elsewhere (none).
  - **`backend/.env.example.txt` removed** — turned out not to be a stale duplicate of `.env.example` but a different, real file (committed since `875c9a6`) containing real PSA MXI URLs and what looked like personal command-history notes, confusingly named like a safe template. Per the user's clarification that its actual purpose was helping other analysts get set up from a fresh clone: merged the genuinely useful, non-secret part (the real, same-for-every-analyst `MXI_STAGE_BASE_URL`/`MXI_PROD_BASE_URL` values) into the canonical `.env.example` — deliberately did NOT carry over its `MXI_ENV=production` default (the canonical file's `MXI_ENV=stage` default is a deliberate safety choice elsewhere in this project) or the personal notes. The file's other genuinely useful content — the production `playwright codegen` recording workflow — was about to be lost entirely, so it was preserved properly: added to `backend/README.md` as a real "Recording a new MXI discovery/codegen script" section, which also surfaced and corrected a real stale claim in `CLAUDE.md` (`saveStorageState.ts` was documented as "Always stage — no --env option," which the tool's own source code directly disproves — it's supported `--env production` for a while). — the privacy-policy piece of `#6`, same audience as `security.md` (PSA IT primarily, readable by engineers/analysts). Grounded in direct code inspection, not boilerplate: confirmed exactly which 4 fields (Order Number, Vendor Name, Current Status, Vendor Notes) get sent to Anthropic's API and nothing else (`inference/anthropicProvider.ts`); confirmed vendor/CRA contact names+emails (`craOwnerName`/`craOwnerEmail`) are used for ESD Finder display/export only and never land in `audit.db` (checked the real `esd_inferences` schema — no such column); confirmed `kpiDb`'s Azure SQL integration exists in the codebase but isn't wired into any running route yet, so it's scaffolding, not an active data flow; confirmed no passenger data of any kind is ever handled by this system. Surfaced two real, previously-undocumented gaps rather than staying silent on them: (1) no account deletion/deactivation feature exists at all (a departed analyst's account and encrypted MXI credential stay active indefinitely unless the local DB is edited directly), and (2) individual `write_up_actions`/`mxi_writes` rows aren't attributed to the logged-in analyst whose session triggered them — only login/logout events are (`auth_events`). Both cross-referenced from `security.md`'s "Known Limitations" table (a new row added there for the account-deletion gap, plus a scope note added to its audit-trail section) so the two documents don't quietly disagree with each other.
- **[#6]** `security.md` (repo root) — the security-documentation piece of `#6`, written for PSA IT review but kept readable for engineers/analysts too. Documents the real, current state of the system (identity model, password/credential storage, session management, the two separate auth boundaries — session vs. automation key, network posture, and an honest "Known Limitations" table for what's deliberately not closed and why) rather than generic boilerplate. Privacy policy and the repository-restructuring proposal are still pending — noted as open items in the doc itself.
- **[#6]** Three real security gaps closed before writing `security.md`, per explicit user direction ("gaps closed where possible... document drafted first, then revised" — the doc below reflects the state *after* these fixes, not before):
  1. **Timing-safe automation-key comparison** (`backend/src/server.ts`'s `requireAutomationKey`) — was a plain `!==` string comparison, which leaks timing information proportional to how many leading characters match. Now `crypto.timingSafeEqual`, guarded by an explicit length check first (`timingSafeEqual` throws on mismatched lengths — the guard is required, not a shortcut).
  2. **Login rate limiting + lockout** (new `backend/src/auth/loginRateLimit.ts`) — login previously had zero brute-force protection (unlimited password guesses, no delay). Now: 5 failed attempts for a given username within 15 minutes locks that username out for 15 minutes, checked *before* password verification (so a locked-out guess never pays the scrypt cost) — confirmed via real test that the 6th attempt is rejected even with the *correct* password, so a lockout can't be distinguished from "about to succeed" by an attacker.
  3. **Auth audit trail** (new `auth_events` table, `backend/src/db/authDb.ts`) — every register/login success/login failure/lockout/logout/password change is now recorded (username + event type + timestamp, never credential material) via `insertAuthEvent`, wired into every relevant handler in `authRoutes.ts`.
  - All three verified with a real 15-check test against the live Express app (correct/wrong/empty/missing automation keys; 4 failed logins still 401, 5th still 401, 6th locked out with 429 even using the real password; `auth_events` contains exactly the expected register/5×login_failure/1×login_locked_out rows, all against the real username) — not just typechecked. Throwaway test script deleted after use.
- **[#6]** Real login/account system — the first concrete piece of the security overhaul, scoped to this session's specific ask (a login page + account creation), not the full `[#6]` item from the original ground rules (security.md, privacy policy, enterprise-grade restructuring are still not started).
  - **New auth database** (`backend/src/db/authDb.ts`, `data/auth.db` — deliberately separate from `data/audit.db`, a different sensitivity class and a smaller, focused table for the user's own planned future Azure migration). One `users` table: `username` (UNIQUE — enforces "no repeat credentials" at the DB level, confirmed with a real duplicate-registration test), `password_hash`, `mxi_password_encrypted`.
  - **Two different crypto primitives for two different jobs** (`backend/src/auth/crypto.ts`, Node's built-in `crypto` only, no new dependency, per explicit user preference): `scrypt` for the login password (one-way, confirmed by the user) — but scrypt can't be reversed, and the MXI writer needs the *real* password to type into MXI's login form, so the same password value is *also* stored via reversible AES-256-GCM under a separate master key (`CREDENTIAL_ENCRYPTION_KEY`, new `.env` var, documented in `.env.example` with the generation command).
  - **One identity, not two**: per explicit user direction, the login username *is* the literal MXI username and the login password *is* the literal MXI password — confirmed no separate "app password" concept anywhere in the design.
  - **Change password** (`backend/src/auth/authService.ts`'s `changePassword`, `/api/auth/change-password`, `src/pages/ChangePassword.tsx`) — requires the current password (not a token/email reset — no email infrastructure exists, and knowing the current password is the right bar here per explicit user direction: "old password, new password, confirm"). Updates both the login hash and the encrypted MXI credential together so they can never drift to different underlying passwords.
  - **Sessions** (`backend/src/auth/sessions.ts`): in-memory (same accepted v1 tradeoff as `jobManager.ts`'s own job registry — a restart logs everyone out, fine for a local tool), httpOnly cookie, `SameSite=Lax`, no `Secure` (this server is 127.0.0.1 plain HTTP by design — a `Secure` cookie would be silently dropped and login would appear to succeed then immediately fail).
  - **`requireSession` replaces `requireAutomationKey`** on every browser-UI route (`/api/vendors`, `/api/discovery`, `/api/execute`, `/api/runs/*`, `/api/esd/*`) — per explicit user confirmation, the Power-Automate-facing endpoints (`/pending-esd-updates`, `/esd-updates/:orderNumber/{approve,reject}`) deliberately keep `AUTOMATION_API_KEY` unchanged, a genuinely different machine-to-machine trust boundary. Verified directly (not assumed) that a valid session alone does NOT satisfy the automation-key gate and vice versa.
  - **Per-user MXI credentials actually reach the Playwright writer.** This required tracing the real call path, not just gating routes: Order Write-Ups' discovery/execute and ESD Finder's write job are spawned CHILD PROCESSES (`jobManager.ts`/`esdFinderJobManager.ts`'s `spawnRunner`) that independently read `MXI_STAGE_USERNAME`/`MXI_STAGE_PASSWORD`/`MXI_PROD_USERNAME`/`MXI_PROD_PASSWORD` from their own inherited environment (`cliMxiClient.ts`) — before this change, every spawned job silently used whatever was in the server's own static `.env`, regardless of who was using the UI. `spawnRunner` gained an optional `envOverrides` parameter (merged over, never replacing, `process.env`), and `startDiscoveryJob`/`startExecuteJob`/`startEsdWriteJob` now require an `MxiCredential` argument, decrypted from the logged-in session's own stored credential (`getMxiCredentialForUser`) and injected as both the STAGE and PROD pairs — "one per user, same for stage and prod," per explicit user direction. The boot-time shared `MxiClient` (`MXI_USERNAME`/`MXI_PASSWORD`, used only by the two Power-Automate endpoints) is untouched, per explicit user direction (CLI tools and that shared client are out of scope — "only the server," and even then not this specific path).
  - **Login/Create Account/Change Password pages** (`src/pages/Login.tsx`, `CreateAccount.tsx`, `ChangePassword.tsx`, new `AuthLayout.tsx` for the first two — no sidebar, since there's nothing to navigate to yet). `src/lib/authContext.tsx` (`AuthProvider`/`useAuth`) + `RequireAuth`/`RequireGuest` route guards in `App.tsx` gate every existing workflow route behind a real session; `/login` and `/create-account` are the only unauthenticated routes. `TopBar.tsx` now shows the real logged-in username (was a hardcoded "Repair Analyst" placeholder) with a user menu (Change Password, Log out).
  - **`lib/api.ts`/`lib/esdFinderApi.ts` stopped sending the bundled `X-Automation-Key`** (was baked into the client JS build, visible in devtools — the browser UI's whole reason that was ever "acceptable" no longer applies once real login exists) — every request now sends `credentials: 'include'` instead, relying on the httpOnly session cookie.
- **[#6]** `TextField` added to `src/components/ui.tsx` (shared input styling for the three new forms) and `PrimaryButton` gained an optional `type` prop (default unchanged `'button'`) — needed because the existing `PrimaryButton` hardcoded `type="button"`, which would have made every one of the three new forms silently fail to submit on click (only Enter-in-a-text-field would have worked). Caught before it shipped, not after.

### Fixed
- **[#6]** Real regression found via an actual browser-driven test (Playwright, not just typecheck) after wiring auth in: `ExecuteRunProvider` (tracks in-progress execute jobs so they survive page reloads) does a one-shot, ref-guarded "re-attach to an already-running job" check on its own first mount, never retried. It used to live at the app root (`main.tsx`), wrapping every route including `/login`. With login now always gating the very first page load, that one-shot check's first (and only) mount was now always anonymous — a silently-swallowed 401, permanently losing the reconnect feature for the rest of the session, every time. Moved `ExecuteRunProvider` into `RequireAuth`'s guarded tree instead, so its first real mount happens exactly when login succeeds.
- **[#6]** **Real, user-reported bug: every action after login (vendor list, ESD Finder file upload, etc.) failed with "Not logged in," even though login/account creation themselves worked.** Root cause: the browser (`localhost:5173`) was calling the backend directly cross-origin (`http://127.0.0.1:3001`) — `localhost` and `127.0.0.1` are different *hosts*, which browsers treat as a different "site" for cookie purposes even though both are loopback. The session cookie's `SameSite=Lax` silently drops on cross-site fetch/XHR calls (only the request that originally sets it — login/register — is unaffected, since that's not about sending an existing cookie). CORS itself was configured correctly the whole time, which is why this wasn't a CORS error — it was a clean, correctly-formed 401. **This was misdiagnosed in this same session's earlier "Verified" notes below as a low-severity, dev-mode-only artifact** — the diagnostic evidence (a request with no Cookie header at all) was right there, but its real-world impact was underestimated. Corrected now, not left standing. Fixed properly: added a Vite dev-server proxy (`vite.config.ts`, `/api` → `http://127.0.0.1:3001`) so the browser only ever talks to its own origin — no cross-site relationship exists at all, regardless of which loopback hostname the app is opened at. `lib/api.ts`/`lib/esdFinderApi.ts`/`lib/authApi.ts`'s `BASE_URL` now defaults to `''` (same-origin relative path) instead of the hardcoded absolute URL; the real `.env.local`'s `VITE_API_BASE_URL` override (which would have silently defeated the proxy fix) was removed, along with the now-fully-unused `VITE_AUTOMATION_API_KEY`. Re-verified with a fresh browser-driven test reproducing the exact reported symptoms (register → Order Write-Ups vendor list renders real rows → ESD Finder file upload reaches real parsing logic, not an auth wall) — zero 401s observed anywhere after login, versus the prior test run which had one at the exact same code path this fix targets.

### Verified (real testing, not just typecheck)
- **[#6]** 13 logic-level checks (register/duplicate-username-rejected/login/wrong-password-rejected/unknown-username-rejected/decrypt-round-trips-exactly/hash-and-ciphertext-never-contain-the-plaintext/change-password-requires-correct-old-password/old-password-stops-working-after-change) against a temp DB, then 22 HTTP-level checks against the real running Express app (session cookie issued on register/login, protected routes 401 without it and 200 with it, logout invalidates it, the automation-key endpoints are provably independent of session auth in both directions) — all passed. Both throwaway scripts deleted after use.
- **[#6]** Full browser-driven walkthrough (Playwright against the real `npm run dev` + `npm run server`, screenshots captured): unauthenticated load redirects to `/login` with no sidebar visible; Create Account redirects into the app; TopBar shows the real username, not the old placeholder; Change Password page renders its 3 fields; Log out redirects to `/login`; re-login with the same credentials succeeds. 9/9 checks passed after the `ExecuteRunProvider` fix above (2 initially failed on a test-script timing race, confirmed via screenshot to be a test issue, not an app bug, then fixed with proper waits and re-confirmed passing). Test account and its `data/auth.db` row deleted afterward — a real user's first account creation starts from a genuinely clean database, not leftover test data.
- ~~One remaining low-severity observation, not chased further: `/api/active-job` occasionally 401s...~~ — **wrong, corrected above.** This was the same cross-origin cookie bug, not a narrow dev-mode timing artifact — it just happened to be the only real fetch call this particular test exercised, so its actual scope (every protected call, every time) wasn't visible yet. See the "Fixed" entry above for the real cause and the real fix.

## 2026-08-12

### Added
- **[#2]** Type-ahead search box in Order Write-Ups' vendor selection (`SelectState` in `src/pages/OrderWriteUps.tsx`), filtering the vendor list by code OR name, case-insensitive. Selection state is keyed by vendor id and untouched by the filter, so narrowing the list never silently deselects an already-picked vendor.
- **[#2]** `code` field added to `VendorListEntry` (`backend/src/api/vendors.ts` and `src/lib/api.ts`) so the frontend can render and search on the raw vendor code separately from its display name. Populated from each `VendorConfig`'s existing `search.vendorCode`.
- **[#1]** New vendor **76863** (Rockwell – Seattle) added to `VENDOR_REGISTRY` (`backend/src/writeUps/shared/vendorRegistry.ts`) — pilot for the new-vendors batch (2N512/Aerotron deferred, too many special rules). REPAIR-default (no BN override), always full flow (`ISSUE_AND_DOCK`), `warrantyEligible: false` — confirmed against the real recording (`discovery-76863-AJS-sn-recording.ts`, found in `C:\Users\717375\Downloads\`, not `backend/` as originally described) that Issue Order + Move to Dock both ran on a non-BN serial, i.e. this vendor simply never uses the warranty path, not a BN-detection edge case. Charge To Account flat `COLLINSDISPATCH100` suffix (Collins-family rule). Purchasing Contact/Conditions/Transportation all inherit the shared family defaults (717375/NET30/FEDEX-2) — the recording's own `232275` was the operator's personal ID, explicitly not a real vendor value, confirmed with the user before use.
- **[#1]** New shared module `backend/src/writeUps/shared/partDetailsReceivingNotes.ts` — the part-level "Details" view (receiving-notes box, `#idContentRow_IdGrpReceivingNotes > td`), distinct from two other same-labeled "Details" views already in this codebase (order-level PO External Reference in `createOrderOnly.ts`; the usage-table "part own details" view in `partOwnDetails.ts`) — never conflated. Gated per-vendor by new `VendorConfig.hasPartDetailsStep` (confirmed present only for 76863 and 1DH10's recordings; confirmed absent from 4X623's, so never assumed universal). Dormant this batch: reads and logs the value but nothing consumes it for a decision yet (that's Aerotron 2N512's rule, deferred).
- **[#1]** RMA framework scaffolding, dormant: `backend/src/writeUps/shared/rmaVendors.ts` (`RMA_VENDOR_IDS` — empty — + `isRmaVendor()`), a new `composeAwaitingRmaNote()` in `createOrderOnly.ts` (reuses the existing `completeCreateOrderOnly`/`verifyExternalReferenceCommitted` order-level External Reference mechanics, just different note text than `composeDoNotShipNote`), and a new `order_created_awaiting_rma` outcome wired through `vendorCodeWriteUp.ts`, `vendorCodeOutcomeLogging.ts`, `api/runLog.ts`, and `db/db.ts`'s outcome union — every exhaustive switch on the outcome type, checked directly rather than assumed. Never fires for any vendor in this batch (empty membership list).

### Fixed (shared engine, not vendor-specific)
- **[#1]** `runVendorCodeWriteUp`'s auth-retry gate (`shared/vendorCodeWriteUp.ts`) was `if (isBnFlow || shipset)` — but `isBnFlow` only reflects whether the BN-prefix *override* fired, and until this batch REPAIR authorization was only ever reached via that override or the shipset case, so the two concepts happened to coincide. 76863 (and the 3 vendors after it) have REPAIR as their *default*, no override involved — `isBnFlow` is false for them even though they need the identical retry-until-APPROVED behavior real BN lines get, not the WARRANTY branch's "REQUESTED is fine" acceptance. Added `isRepairFlow = resolved.authFlow === AUTH_FLOW_REPAIR` (a strict superset of `isBnFlow` — every BN-override line already resolves to REPAIR by construction) and switched the gate to it. Zero behavior change for every existing vendor (0T1Y4 family, Skypaxxx); required for 76863 to reach Issue Order/Move to Dock correctly.

### Changed
- **[#2]** Hardcoded real vendor names as `displayName` for all 10 entries in `backend/src/writeUps/shared/vendorRegistry.ts` (previously placeholder text like `"Vendor 0T1Y4"`), per an explicit code→name mapping supplied by the user. No file path, no env var, no Vendor Assignments sheet parsing, no unmatched-code fallback — the original plan to read this at runtime from the Vendor Assignments spreadsheet was scrapped in favor of a static mapping.
- **[#2]** `OrderWriteUps.tsx`'s vendor rows now render `<code> — <name>` (code in accent color) instead of name alone.

### Fixed
- **[#3]** MXI writer's Playwright browser now launches headless by default (was always `headless: false`, the stray visible-browser icon). `chromium.launch()` in `backend/src/mxiWriter/mxiClient.ts:59` now reads `this.config.headless`. Added `HEADLESS` env var (default `true`; set to the exact literal `"false"` to watch the browser for debugging), documented in `.env.example`. `MxiConfig` gained a `headless` field; both places that construct one (`config.ts`'s `loadMxiConfig()` for the server, and `cliMxiClient.ts` for the `mxi:read-esd`/`mxi:write-esd`/`approve-and-write` CLI tools) now share one `readHeadlessFlag()` helper instead of duplicating the env-var check. Confirmed via grep this is still the only `.launch(` call site in `backend/src`.
- **[#4a]** ESD Finder file ingestion (`backend/src/api/esdFinder/ingestion.ts`) no longer fails real dropped files. Root cause was NOT a title-row-position issue (that theory, from a synthetic test file, didn't hold up against a real one) — it was two separate real bugs, confirmed against a genuine file (`08-12-2026 Brayden Bury OOR.xlsm`, sheet `VendorOrders`, 1933 rows):
  1. **Hardcoded sheet-name lookup.** `readRealHeaders()` called `getWorksheet('Vendor OOR')` / `getWorksheet('CRA OOR')` literally — a real export named its sheet `VendorOrders` instead, so the lookup returned `undefined` and the old code silently handed back an empty header set (every required column then reported "missing," regardless of actual content). Replaced with `resolveAndValidateSheet()`: resolves the real data sheet by CONTENT (whichever worksheet's row-1 headers, after aliasing, satisfy the required column list for the schema being checked), falling back to the first/only worksheet if no sheet matches by content. The silent empty-Set early-return is gone — if a workbook has no worksheets at all, it now throws a clear error naming what it looked for. The previously-planned "scan first 5 rows for a title row" idea was dropped entirely per the user — row 1 is confirmed to always be the true header row.
  2. **Header-name drift.** The real file's CRA-shaped headers (`TAT`, `Order Status`, `Notes`, `RO ESD`) match `CRA_OOR_REQUIRED_HEADERS` exactly except `RO ESD` where the code expects `MXI RO ESD`. Added `CRA_OOR_HEADER_ALIASES` (`backend/src/parsers/craOorParser.ts`) — a small, explicit `{ 'RO ESD': 'MXI RO ESD' }` map — and threaded an optional `headerAliases` parameter through `readSheetRows()` (`sheetUtils.ts`, default `{}`, so every other caller — Vendor OOR, Vendor Assignments, the CLI path — is unaffected) so the alias applies both at validation time AND at actual data-parse time. Verified this matters: without threading it into the parse itself, `parseCraOor`'s `get('MXI RO ESD')` would have returned `null` for every row even after validation passed — the exact "looks complete, one field is silently null" failure mode this project has been burned by before. Confirmed via a synthetic single-row test that the alias genuinely carries a populated value through (`"RO ESD": "15-AUG-2026"` → `mxiRoEsd: "15-AUG-2026"`), not just structurally-present-but-null. The alias is scoped to CRA OOR only — Vendor OOR's own legitimate `RO ESD` column (a different field, the vendor's promised ship date) is never aliased.
  3. **Type detection.** `resolveAndValidateSheet()` also tries the *other* known OOR schema before giving up, so a CRA-shaped file validated under `role="vendor"` now gets a specific "expected VENDOR OOR but this matches CRA OOR instead (sheet X)" error instead of a generic wall of "10 headers missing."
  - Real-file re-test after the fix: `role="cra"` now resolves sheet `VendorOrders` by content, applies the alias, and parses 1933 rows successfully (`role="vendor"` correctly still rejects it, now with the specific cross-schema message). This file's real `RO ESD` column happens to be empty on every row (confirmed independently against the raw sheet, not just the parser output) — so this is a genuine property of the file, not a residual bug.
  - `parseVendorOorFileWithValidation`/`parseCraOorFileWithValidation` also stopped independently re-guessing a hardcoded sheet name for the actual data parse — they now parse the exact sheet validation just resolved.
  - Temp diagnostic logging (gated behind `ESD_FINDER_DEBUG_HEADERS=1`) stays in `ingestion.ts`, moved into `resolveAndValidateSheet()` and fixed to always fire before any resolution decision (previously it sat after the early-return and could be silently skipped in exactly the failure case that mattered).
  - Real vendor data files (`08-12-2026 Brayden Bury OOR.xlsm`, `Week of 08-10-2026 MEASURETECH INC OOR.xlsx`) ended up directly under `backend/` this session (outside the gitignored `data/` dir) — flagged to the user, not staged, `.gitignore` fix or manual move still pending.

### Upcoming
- **[#1]** New vendors + Skypaxxx non-shipset case. Deferred — user will provide a separate, dedicated prompt. Context: the non-shipset case runs all-in-one under a single vendor selection, the same way selecting a vendor code today fires its batch + warranty cases together.
- **[#6]** Security overhaul (login portal, hashed local accounts, encrypted per-user MXI credentials, migration-ready toward Azure). Biggest lift, implemented last. Targets identified: `backend/.env` / `.env.example`, `backend/src/server.ts` (no auth today besides the shared `X-Automation-Key` header), `backend/src/mxiWriter/config.ts` / `cliMxiClient.ts`. Short plan + questions still to be delivered before any code is written, per the user's own instruction on this item.
